# Mejoras v2 — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar 4 mejoras acopladas: dedup de comparaciones por set de versionIds, filtros extendidos y orden en catálogo, mover disclaimer de Comparar a icono info, y sistema de favoritos end-to-end con pantalla `/favoritos` y carrusel en Comparar.

**Architecture:** Backend-first (Postgres + Prisma) con migración que crea `Favorite` y agrega `versionsHash` a `Comparison` con índice único. Frontend Angular con `FavoritesStore` (signals) que sincroniza con auth vía `effect()`. UI extiende `VehicleCardComponent` con botón corazón. Pantalla Favoritos reutiliza el card.

**Tech Stack:** Angular 20 (signals, OnPush, lazy loading), Express + Zod + Prisma/Postgres, Vitest + supertest (BE) / HttpClientTesting (FE).

**Spec de referencia:** `docs/superpowers/specs/2026-07-01-mejoras-v2-design.md`

## Global Constraints

- Cada task termina con commit (`git add -A && git commit -m "<scope>: <desc>"`).
- Backend: `npm -w apps/backend run test` debe pasar antes de commitear.
- Frontend: `npm -w apps/frontend run test` debe pasar antes de commitear.
- TypeScript estricto. No agregar `any` implícito. Reutilizar `shared/errors` (`AppError`, `conflict`, `badRequest`, `notFound`, `unauthorized`, `validation`).
- Naming: archivos en kebab-case; clases en PascalCase; métodos en camelCase.
- Todas las UI nuevas usan clases Tailwind ya presentes en `apps/frontend/src/styles.css` (variables `brand-*`, `surface`, `ink`, `border`, `warn`).
- Iconos: `material-symbols-outlined`. No instalar nuevas libs de iconos.
- Auth: cualquier endpoint `/me/*` debe pasar por `authenticate` middleware.

---

## File map (qué hace cada archivo nuevo/modificado)

| Archivo | Cambio | Responsabilidad |
|---|---|---|
| `apps/backend/prisma/schema.prisma` | mod | Modelo `Favorite`, relación `User.favorites`, `Model.favorites`, campo `Comparison.versionsHash`, `@@unique([userId, versionsHash])` |
| `apps/backend/prisma/migrations/<ts>_favorites_and_versions_hash/migration.sql` | crear | CREATE TABLE Favorite, ALTER TABLE Comparison, backfill versionsHash, unique index |
| `apps/backend/src/shared/errors.ts` | mod | `conflict` acepta details opcionales |
| `apps/backend/src/shared/response.ts` | mod | `fail()` propaga `details` |
| `apps/backend/src/app.ts` | mod | Handler de errores propaga `details`; registrar `meFavoritesRouter` |
| `apps/backend/src/modules/comparisons/comparisons.service.ts` | mod | `create()` calcula `versionsHash`, pre-check por unique, lanza `conflict({code:'COMPARISON_DUPLICATE', slug})` |
| `apps/backend/src/modules/models/models.dto.ts` | mod | Schema agrega `sort`, `order` |
| `apps/backend/src/modules/models/models.service.ts` | mod | Enriquecer `minConsumption`; `.sort()` en memoria |
| `apps/backend/src/modules/favorites/favorites.{service,controller,routes}.ts` | crear | CRUD de favoritos |
| `apps/backend/src/modules/favorites/*.spec.ts` | crear | Tests |
| `apps/frontend/src/app/core/favorites-store.service.ts` | crear | Signal store + effect reactivo a auth |
| `apps/frontend/src/app/shared/ui/vehicle-card.component.{ts,html}` | mod | Input `isFavorite`, output `favoriteToggled`, botón corazón |
| `apps/frontend/src/app/features/catalog/catalog.component.{ts,html}` | mod | Filtros extendidos, sort/order, marcas |
| `apps/frontend/src/app/features/landing/landing.component.{html,ts}` | mod | Cablear favoritos en cards |
| `apps/frontend/src/app/features/compare/compare.component.{ts,html}` | mod | Dedup handler, info icon, carrusel favoritos |
| `apps/frontend/src/app/features/favorites/favorites.component.{ts,html,css}` | crear | Pantalla Favoritos |
| `apps/frontend/src/app/app.routes.ts` | mod | Ruta `/favoritos` |
| `apps/frontend/src/app/layout/top-nav-bar.component.{ts,html}` | mod | Link Favoritos |
| `apps/frontend/src/app/app.config.ts` | (verificar) | Asegurar que `AuthService.bootstrap()` se llame al inicio |
| `*.spec.ts` afectados | mod | Tests adicionales |

---

## Fase 0 — Schema y migración

### Task 0: Migración Prisma — modelo Favorite + versionsHash

**Files:**
- Modify: `apps/backend/prisma/schema.prisma`
- Create: `apps/backend/prisma/migrations/<timestamp>_favorites_and_versions_hash/migration.sql` (Prisma genera el timestamp al aplicar)

**Interfaces:**
- Consumes: schema actual
- Produces: tabla `Favorite`, columna `Comparison.versionsHash`, unique `[userId, versionsHash]`

- [ ] **Step 1: Modificar schema.prisma**

Agregar al final del archivo (después de `ComparisonItem`):

```prisma
model Favorite {
  id        String   @id @default(cuid())
  userId    String
  modelId   String
  createdAt DateTime @default(now())
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  model     Model    @relation(fields: [modelId], references: [id], onDelete: Cascade)

  @@unique([userId, modelId])
  @@index([userId])
}
```

Modificar `User` para agregar relación:
```prisma
model User {
  // ...campos existentes...
  favorites  Favorite[]
}
```

Modificar `Model` para agregar relación:
```prisma
model Model {
  // ...campos existentes...
  favorites Favorite[]
}
```

Modificar `Comparison`:
```prisma
model Comparison {
  id           String           @id @default(cuid())
  userId       String
  slug         String?          @unique
  name         String?
  versionsHash String
  createdAt    DateTime         @default(now())
  user         User             @relation(fields: [userId], references: [id], onDelete: Cascade)
  items        ComparisonItem[]

  @@unique([userId, versionsHash])
  @@index([userId])
}
```

- [ ] **Step 2: Generar migración**

Run:
```bash
npm -w apps/backend run db:migrate -- --name favorites_and_versions_hash
```

Si la migración autogenerada no incluye el backfill de `versionsHash`, **editarla manualmente** antes de aplicarla. Agregar SQL de backfill (Postgres + pgcrypto):

```sql
-- Backfill versionsHash desde ComparisonItem
CREATE EXTENSION IF NOT EXISTS pgcrypto;

UPDATE "Comparison" c
SET "versionsHash" = encode(digest(
  (SELECT string_agg(ci."versionId", ',' ORDER BY ci."versionId")
   FROM "ComparisonItem" ci WHERE ci."comparisonId" = c.id),
  'sha1'
), 'hex')
WHERE c."versionsHash" = '' OR c."versionsHash" IS NULL;
```

Si pgcrypto no se puede crear (permisos), fallback: dejar `versionsHash` con default `''` y ejecutar script Node `apps/backend/scripts/backfill-versions-hash.ts` después de la migración. El script debe:

```ts
import { PrismaClient } from "@prisma/client";
import crypto from "node:crypto";
const prisma = new PrismaClient();

async function main() {
  const all = await prisma.comparison.findMany({ include: { items: true } });
  for (const c of all) {
    const sorted = c.items.map(i => i.versionId).sort();
    const hash = crypto.createHash("sha1").update(sorted.join(",")).digest("hex");
    await prisma.comparison.update({ where: { id: c.id }, data: { versionsHash: hash } });
  }
}
main().finally(() => prisma.$disconnect());
```

- [ ] **Step 3: Verificar migración y schema**

Run:
```bash
npm -w apps/backend run db:reset
npm -w apps/backend run db:seed
```

Expected: la BD se resetea y aplica la migración limpia, luego el seed puebla datos.

- [ ] **Step 4: Commit**

```bash
git add apps/backend/prisma
git commit -m "chore(db): add Favorite model and Comparison.versionsHash"
```

---

## Fase 1 — Backend (dedup + sort + favorites module)

### Task 1: Extender `conflict()` y `fail()` para aceptar `details`

**Files:**
- Modify: `apps/backend/src/shared/errors.ts`
- Modify: `apps/backend/src/shared/response.ts`
- Modify: `apps/backend/src/app.ts`

- [ ] **Step 1: Cambiar `conflict()` para aceptar details**

En `apps/backend/src/shared/errors.ts`, reemplazar:

```ts
export const conflict = (msg: string) => new AppError("CONFLICT", msg);
```

por:

```ts
export const conflict = (msg: string, details?: Record<string, unknown>) =>
  Object.assign(new AppError("CONFLICT", msg), { details });
```

- [ ] **Step 2: Hacer que `AppError` lleve `details`**

En el mismo archivo, modificar la clase `AppError`:

```ts
export class AppError extends Error {
  readonly details?: Record<string, unknown>;
  constructor(
    public readonly code: ErrorCode,
    message: string,
    details?: Record<string, unknown>,
  ) {
    super(message);
    this.name = "AppError";
    this.details = details;
  }
  get status(): number {
    return STATUS[this.code];
  }
}
```

Y los helpers que reciben details:

```ts
export const notFound = (msg = "Recurso no encontrado") =>
  new AppError("NOT_FOUND", msg);
export const unauthorized = (msg = "No autenticado") =>
  new AppError("UNAUTHORIZED", msg);
export const conflict = (msg: string, details?: Record<string, unknown>) =>
  new AppError("CONFLICT", msg, details);
export const validation = (msg: string) => new AppError("VALIDATION", msg);
export const badRequest = (msg: string) => new AppError("BAD_REQUEST", msg);
```

- [ ] **Step 3: Actualizar `fail()` para incluir details**

En `apps/backend/src/shared/response.ts`:

```ts
import type { AppError } from "./errors.js";

export const ok = <T>(data: T) => ({ data, error: null });
export const fail = (err: AppError) => ({
  data: null,
  error: { code: err.code, message: err.message, ...(err.details ?? {}) },
});
```

- [ ] **Step 4: Propagar details en el handler global de errores**

En `apps/backend/src/app.ts`, reemplazar el middleware de error (líneas 47-53):

```ts
app.use((err: unknown, _req: express.Request, res: express.Response, _next: express.NextFunction) => {
  if (err && typeof err === "object" && "code" in err && "message" in err) {
    const e = err as { code: string; message: string; status?: number; details?: Record<string, unknown> };
    return res.status(e.status ?? 500).json({
      data: null,
      error: { code: e.code, message: e.message, ...(e.details ?? {}) },
    });
  }
  return res.status(500).json({ data: null, error: { code: "INTERNAL", message: "Error interno" } });
});
```

- [ ] **Step 5: Verificar que tests existentes siguen pasando**

Run: `npm -w apps/backend run test`
Expected: todos los tests pasan (no rompimos nada; `details` es opcional).

- [ ] **Step 6: Commit**

```bash
git add apps/backend/src/shared apps/backend/src/app.ts
git commit -m "feat(be): extend error helper with optional details"
```

---

### Task 2: `ComparisonsService.create()` con dedup

**Files:**
- Modify: `apps/backend/src/modules/comparisons/comparisons.service.ts`
- Modify: `apps/backend/src/modules/comparisons/comparisons.controller.spec.ts`

- [ ] **Step 1: Agregar import de `crypto` y `conflict`**

En `apps/backend/src/modules/comparisons/comparisons.service.ts`, agregar al inicio:

```ts
import crypto from "node:crypto";
```

(El import de `conflict` se agrega donde se usa; ya hay `import { badRequest, notFound } from "../../shared/errors.js";` — extenderlo a `import { badRequest, conflict, notFound } from "../../shared/errors.js";`).

- [ ] **Step 2: Modificar `create()` para calcular versionsHash y pre-check**

Reemplazar todo el método `create` por:

```ts
async create({ userId, versionIds, name }: { userId: string; versionIds: string[]; name?: string }) {
  if (versionIds.length < 1 || versionIds.length > 3) throw badRequest("Compara entre 1 y 3 versiones");

  const sorted = [...versionIds].sort();
  const versionsHash = crypto.createHash("sha1").update(sorted.join(",")).digest("hex");

  const existing = await this.prisma.comparison.findUnique({
    where: { userId_versionsHash: { userId, versionsHash } },
    select: { slug: true },
  });
  if (existing) {
    throw conflict("Ya existe una comparación guardada con esos modelos", {
      code: "COMPARISON_DUPLICATE",
      slug: existing.slug ?? "",
    });
  }

  const data = {
    userId,
    versionsHash,
    ...(name !== undefined ? { name } : {}),
    items: { create: versionIds.map((versionId, i) => ({ versionId, position: i + 1 })) },
  };
  let lastError: unknown;
  for (let attempt = 0; attempt < SLUG_RETRY_LIMIT; attempt++) {
    try {
      const cmp = await this.prisma.comparison.create({
        data: { ...data, slug: slugger() },
      });
      return { id: cmp.id, slug: cmp.slug ?? "" };
    } catch (e) {
      lastError = e;
      if (!(e instanceof Prisma.PrismaClientKnownRequestError) || e.code !== "P2002") throw e;
    }
  }
  throw lastError;
}
```

- [ ] **Step 3: Agregar test 409 al controller spec**

Abrir `apps/backend/src/modules/comparisons/comparisons.controller.spec.ts` y agregar al final del `describe` (antes del cierre):

```ts
it("segundo POST con mismos versionIds retorna 409 con slug existente", async () => {
  const v = await prisma.brand.create({ data: { name: "Dup" } }).then((br) =>
    prisma.model.create({ data: { brandId: br.id, name: "M", segment: "SEDAN" } })).then((m) =>
    prisma.version.create({ data: { modelId: m.id, name: "x", year: 2026, priceClp: 1, transmission: "MANUAL", fuel: "BENCINA",
      engineDisplacementCc: 1, powerHp: 1, torqueNm: 1, consumptionCityKmL: 1, consumptionHighwayKmL: 1,
      lengthMm: 1, widthMm: 1, heightMm: 1, weightKg: 1, trunkLiters: 1, airbagCount: 1,
      hasAbs: true, hasEsp: true, hasCruiseControl: true } }));
  const registered = await request(createApp())
    .post("/api/v1/auth/register")
    .send({ email: "dup" + "@" + "test.cl", password: "secreto123", name: "Dup" });
  expect(registered.status).toBe(200);
  const cookie = registered.headers["set-cookie"]?.[0] as string;

  const first = await request(createApp())
    .post("/api/v1/me/comparisons")
    .set("Cookie", cookie)
    .send({ versionIds: [v.id] });
  expect(first.status).toBe(200);
  const slug = first.body.data.slug as string;

  const second = await request(createApp())
    .post("/api/v1/me/comparisons")
    .set("Cookie", cookie)
    .send({ versionIds: [v.id] });
  expect(second.status).toBe(409);
  expect(second.body.error.code).toBe("COMPARISON_DUPLICATE");
  expect(second.body.error.slug).toBe(slug);
});
```

- [ ] **Step 4: Run tests**

Run: `npm -w apps/backend run test -- comparisons`
Expected: PASS, incluido el nuevo test.

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/modules/comparisons
git commit -m "feat(be): dedup comparisons by versionsHash with 409 response"
```

---

### Task 3: `FavoritesService`

**Files:**
- Create: `apps/backend/src/modules/favorites/favorites.service.ts`
- Create: `apps/backend/src/modules/favorites/favorites.service.spec.ts`

- [ ] **Step 1: Crear `favorites.service.ts`**

```ts
import type { PrismaClient } from "@prisma/client";
import { notFound } from "../../shared/errors.js";

export interface FavoriteModelCard {
  id: string;
  name: string;
  segment: string;
  brand: { id: string; name: string };
  imageUrl: string | null;
  minPrice: number | null;
  defaultVersion: { id: string; name: string; priceClp: number; year: number } | null;
  versions: Array<{
    id: string;
    name: string;
    year: number;
    priceClp: number;
    transmission: string | null;
    fuel: string | null;
    engineDisplacementCc: number | null;
    powerHp: number | null;
    torqueNm: number | null;
    consumptionCityKmL: number | null;
    consumptionHighwayKmL: number | null;
  }>;
}

export class FavoritesService {
  constructor(private readonly prisma: PrismaClient) {}

  async listIds(userId: string): Promise<string[]> {
    const rows = await this.prisma.favorite.findMany({
      where: { userId },
      select: { modelId: true },
      orderBy: { createdAt: "desc" },
    });
    return rows.map((r) => r.modelId);
  }

  async listModels(userId: string): Promise<FavoriteModelCard[]> {
    const favs = await this.prisma.favorite.findMany({
      where: { userId },
      include: {
        model: {
          include: {
            brand: true,
            versions: { orderBy: { priceClp: "asc" } },
          },
        },
      },
      orderBy: { createdAt: "desc" },
    });
    return favs.map((f) => this.toCard(f.model));
  }

  async add(userId: string, modelId: string): Promise<{ created: boolean }> {
    const model = await this.prisma.model.findUnique({ where: { id: modelId } });
    if (!model) throw notFound("Modelo no encontrado");
    try {
      await this.prisma.favorite.create({ data: { userId, modelId } });
      return { created: true };
    } catch (e) {
      // unique constraint -> ya es favorito, idempotente
      const err = e as { code?: string };
      if (err.code === "P2002") return { created: false };
      throw e;
    }
  }

  async remove(userId: string, modelId: string): Promise<void> {
    await this.prisma.favorite.deleteMany({ where: { userId, modelId } });
  }

  private toCard(m: {
    id: string;
    name: string;
    segment: string;
    imageUrl: string | null;
    galleryUrls: string[];
    brand: { id: string; name: string };
    versions: Array<{
      id: string;
      name: string;
      year: number;
      priceClp: number;
      transmission: string;
      fuel: string;
      engineDisplacementCc: number;
      powerHp: number;
      torqueNm: number;
      consumptionCityKmL: number;
      consumptionHighwayKmL: number;
    }>;
  }): FavoriteModelCard {
    const prices = m.versions.map((v) => v.priceClp);
    const minPrice = prices.length ? Math.min(...prices) : null;
    const firstVersion = m.versions[0];
    return {
      id: m.id,
      name: m.name,
      segment: m.segment,
      brand: { id: m.brand.id, name: m.brand.name },
      imageUrl: m.galleryUrls[0] ?? m.imageUrl ?? null,
      minPrice,
      defaultVersion: firstVersion
        ? { id: firstVersion.id, name: firstVersion.name, priceClp: firstVersion.priceClp, year: firstVersion.year }
        : null,
      versions: m.versions.map((v) => ({
        id: v.id,
        name: v.name,
        year: v.year,
        priceClp: v.priceClp,
        transmission: v.transmission,
        fuel: v.fuel,
        engineDisplacementCc: v.engineDisplacementCc,
        powerHp: v.powerHp,
        torqueNm: v.torqueNm,
        consumptionCityKmL: v.consumptionCityKmL,
        consumptionHighwayKmL: v.consumptionHighwayKmL,
      })),
    };
  }
}
```

- [ ] **Step 2: Crear `favorites.service.spec.ts`**

```ts
import { describe, it, expect, beforeEach } from "vitest";
import { FavoritesService } from "./favorites.service.js";
import { setupTestPrisma, resetTestDb } from "../../../__tests__/helpers/db.js";
import { prisma } from "../../infra/prisma.js";

describe("FavoritesService", () => {
  beforeEach(async () => { setupTestPrisma(); await resetTestDb(prisma); });

  it("add crea favorito; segundo add es idempotente", async () => {
    const svc = new FavoritesService(prisma);
    const u = await prisma.user.create({ data: { email: "u" + "@" + "cualautocompro.cl", name: "U", passwordHash: "x" } });
    const b = await prisma.brand.create({ data: { name: "B" } });
    const m = await prisma.model.create({ data: { brandId: b.id, name: "M", segment: "SEDAN" } });

    const r1 = await svc.add(u.id, m.id);
    expect(r1.created).toBe(true);
    const r2 = await svc.add(u.id, m.id);
    expect(r2.created).toBe(false);

    const ids = await svc.listIds(u.id);
    expect(ids).toEqual([m.id]);
  });

  it("remove quita favorito; segundo remove no falla", async () => {
    const svc = new FavoritesService(prisma);
    const u = await prisma.user.create({ data: { email: "u" + "@" + "cualautocompro.cl", name: "U", passwordHash: "x" } });
    const b = await prisma.brand.create({ data: { name: "B" } });
    const m = await prisma.model.create({ data: { brandId: b.id, name: "M", segment: "SEDAN" } });
    await svc.add(u.id, m.id);
    await svc.remove(u.id, m.id);
    const ids = await svc.listIds(u.id);
    expect(ids).toEqual([]);
    await expect(svc.remove(u.id, m.id)).resolves.toBeUndefined();
  });

  it("listModels retorna shape correcto con versiones", async () => {
    const svc = new FavoritesService(prisma);
    const u = await prisma.user.create({ data: { email: "u" + "@" + "cualautocompro.cl", name: "U", passwordHash: "x" } });
    const b = await prisma.brand.create({ data: { name: "T" } });
    const m = await prisma.model.create({ data: { brandId: b.id, name: "Yaris", segment: "HATCHBACK" } });
    await prisma.version.create({
      data: { modelId: m.id, name: "XLS", year: 2026, priceClp: 14990000, transmission: "AUTOMATIC", fuel: "BENCINA",
        engineDisplacementCc: 1500, powerHp: 120, torqueNm: 145, consumptionCityKmL: 14, consumptionHighwayKmL: 18,
        lengthMm: 4200, widthMm: 1760, heightMm: 1480, weightKg: 1100, trunkLiters: 360, airbagCount: 6,
        hasAbs: true, hasEsp: true, hasCruiseControl: true },
    });
    await svc.add(u.id, m.id);

    const cards = await svc.listModels(u.id);
    expect(cards).toHaveLength(1);
    expect(cards[0]?.name).toBe("Yaris");
    expect(cards[0]?.brand.name).toBe("T");
    expect(cards[0]?.minPrice).toBe(14990000);
    expect(cards[0]?.versions).toHaveLength(1);
    expect(cards[0]?.versions[0]?.powerHp).toBe(120);
  });

  it("add con modelId inexistente lanza NOT_FOUND", async () => {
    const svc = new FavoritesService(prisma);
    const u = await prisma.user.create({ data: { email: "u" + "@" + "cualautocompro.cl", name: "U", passwordHash: "x" } });
    await expect(svc.add(u.id, "no-existe")).rejects.toMatchObject({ code: "NOT_FOUND" });
  });

  it("listIds está aislado por usuario", async () => {
    const svc = new FavoritesService(prisma);
    const a = await prisma.user.create({ data: { email: "a" + "@" + "cualautocompro.cl", name: "A", passwordHash: "x" } });
    const b = await prisma.user.create({ data: { email: "b" + "@" + "cualautocompro.cl", name: "B", passwordHash: "x" } });
    const br = await prisma.brand.create({ data: { name: "X" } });
    const m = await prisma.model.create({ data: { brandId: br.id, name: "M", segment: "SEDAN" } });
    await svc.add(a.id, m.id);
    expect(await svc.listIds(a.id)).toEqual([m.id]);
    expect(await svc.listIds(b.id)).toEqual([]);
  });
});
```

- [ ] **Step 3: Verificar existencia de helper de test**

```bash
ls apps/backend/__tests__/helpers/db.ts
```

Si no existe, ver `comparisons.service.spec.ts` que ya lo usa — el helper ya está. Si falta, copiar de comparisons.

- [ ] **Step 4: Run tests**

Run: `npm -w apps/backend run test -- favorites`
Expected: PASS, 5 tests.

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/modules/favorites
git commit -m "feat(be): add FavoritesService with idempotent add"
```

---

### Task 4: `FavoritesController` + routes + registrar en `app.ts`

**Files:**
- Create: `apps/backend/src/modules/favorites/favorites.controller.ts`
- Create: `apps/backend/src/modules/favorites/favorites.routes.ts`
- Create: `apps/backend/src/modules/favorites/favorites.controller.spec.ts`
- Modify: `apps/backend/src/app.ts`

- [ ] **Step 1: Crear `favorites.controller.ts`**

```ts
import { z } from "zod";
import type { Request, Response } from "express";
import { ah } from "../../shared/async-handler.js";
import { ok } from "../../shared/response.js";
import { prisma } from "../../infra/prisma.js";
import { unauthorized, validation } from "../../shared/errors.js";
import { FavoritesService } from "./favorites.service.js";

const svc = new FavoritesService(prisma);
const addSchema = z.object({ modelId: z.string().min(1) });

export const favoritesController = {
  listIds: ah(async (req: Request, res: Response) => {
    const u = req.user; if (!u) throw unauthorized();
    res.json(ok({ modelIds: await svc.listIds(u.id) }));
  }),

  listModels: ah(async (req: Request, res: Response) => {
    const u = req.user; if (!u) throw unauthorized();
    res.json(ok(await svc.listModels(u.id)));
  }),

  add: ah(async (req: Request, res: Response) => {
    const u = req.user; if (!u) throw unauthorized();
    const parsed = addSchema.safeParse(req.body);
    if (!parsed.success) throw validation(parsed.error.issues.map((i) => i.message).join("; "));
    const { modelId, created } = await svc.add(u.id, parsed.data.modelId);
    res.json(ok({ modelId, created }));
  }),

  remove: ah(async (req: Request, res: Response) => {
    const u = req.user; if (!u) throw unauthorized();
    const modelId = req.params.modelId ?? "";
    if (!modelId) throw validation("modelId requerido");
    await svc.remove(u.id, modelId);
    res.json(ok({ removed: true }));
  }),
};
```

- [ ] **Step 2: Crear `favorites.routes.ts`**

```ts
import { Router } from "express";
import { authenticate } from "../auth/auth.middleware.js";
import { favoritesController } from "./favorites.controller.js";

export const meFavoritesRouter = Router();
meFavoritesRouter.use(authenticate);
meFavoritesRouter.get("/", favoritesController.listIds);
meFavoritesRouter.get("/models", favoritesController.listModels);
meFavoritesRouter.post("/", favoritesController.add);
meFavoritesRouter.delete("/:modelId", favoritesController.remove);
```

- [ ] **Step 3: Crear `favorites.controller.spec.ts`**

```ts
import { describe, it, expect, beforeEach } from "vitest";
import request from "supertest";
import { createApp } from "../../app.js";
import { setupTestPrisma, resetTestDb } from "../../../__tests__/helpers/db.js";
import { prisma } from "../../infra/prisma.js";

describe("Favorites endpoints", () => {
  beforeEach(async () => {
    setupTestPrisma();
    await resetTestDb(prisma);
  });

  it("GET /me/favorites sin auth retorna 401", async () => {
    const res = await request(createApp()).get("/api/v1/me/favorites");
    expect(res.status).toBe(401);
  });

  it("POST es idempotente: 2º call devuelve 200 con created=false", async () => {
    const reg = await request(createApp())
      .post("/api/v1/auth/register")
      .send({ email: "fav" + "@" + "test.cl", password: "secreto123", name: "Fav" });
    const cookie = reg.headers["set-cookie"]?.[0] as string;
    const b = await prisma.brand.create({ data: { name: "T" } });
    const m = await prisma.model.create({ data: { brandId: b.id, name: "Yaris", segment: "HATCHBACK" } });

    const r1 = await request(createApp())
      .post("/api/v1/me/favorites")
      .set("Cookie", cookie)
      .send({ modelId: m.id });
    expect(r1.status).toBe(200);
    expect(r1.body.data.created).toBe(true);

    const r2 = await request(createApp())
      .post("/api/v1/me/favorites")
      .set("Cookie", cookie)
      .send({ modelId: m.id });
    expect(r2.status).toBe(200);
    expect(r2.body.data.created).toBe(false);
  });

  it("DELETE quita y GET /models ya no lo incluye", async () => {
    const reg = await request(createApp())
      .post("/api/v1/auth/register")
      .send({ email: "fav2" + "@" + "test.cl", password: "secreto123", name: "Fav2" });
    const cookie = reg.headers["set-cookie"]?.[0] as string;
    const b = await prisma.brand.create({ data: { name: "T" } });
    const m = await prisma.model.create({ data: { brandId: b.id, name: "Yaris", segment: "HATCHBACK" } });

    await request(createApp()).post("/api/v1/me/favorites").set("Cookie", cookie).send({ modelId: m.id });
    const del = await request(createApp()).delete(`/api/v1/me/favorites/${m.id}`).set("Cookie", cookie);
    expect(del.status).toBe(200);
    expect(del.body.data.removed).toBe(true);

    const list = await request(createApp()).get("/api/v1/me/favorites/models").set("Cookie", cookie);
    expect(list.status).toBe(200);
    expect(list.body.data).toEqual([]);
  });

  it("GET /models retorna shape esperado con brand y versions", async () => {
    const reg = await request(createApp())
      .post("/api/v1/auth/register")
      .send({ email: "fav3" + "@" + "test.cl", password: "secreto123", name: "Fav3" });
    const cookie = reg.headers["set-cookie"]?.[0] as string;
    const b = await prisma.brand.create({ data: { name: "T" } });
    const m = await prisma.model.create({ data: { brandId: b.id, name: "Yaris", segment: "HATCHBACK" } });
    await prisma.version.create({
      data: { modelId: m.id, name: "XLS", year: 2026, priceClp: 14990000, transmission: "AUTOMATIC", fuel: "BENCINA",
        engineDisplacementCc: 1500, powerHp: 120, torqueNm: 145, consumptionCityKmL: 14, consumptionHighwayKmL: 18,
        lengthMm: 4200, widthMm: 1760, heightMm: 1480, weightKg: 1100, trunkLiters: 360, airbagCount: 6,
        hasAbs: true, hasEsp: true, hasCruiseControl: true },
    });
    await request(createApp()).post("/api/v1/me/favorites").set("Cookie", cookie).send({ modelId: m.id });

    const res = await request(createApp()).get("/api/v1/me/favorites/models").set("Cookie", cookie);
    expect(res.status).toBe(200);
    expect(res.body.data[0]).toMatchObject({
      name: "Yaris",
      brand: { name: "T" },
      minPrice: 14990000,
      versions: [expect.objectContaining({ powerHp: 120 })],
    });
  });

  it("POST con modelId inexistente retorna 404", async () => {
    const reg = await request(createApp())
      .post("/api/v1/auth/register")
      .send({ email: "fav4" + "@" + "test.cl", password: "secreto123", name: "Fav4" });
    const cookie = reg.headers["set-cookie"]?.[0] as string;
    const res = await request(createApp())
      .post("/api/v1/me/favorites")
      .set("Cookie", cookie)
      .send({ modelId: "no-existe" });
    expect(res.status).toBe(404);
  });
});
```

- [ ] **Step 4: Registrar router en `app.ts`**

En `apps/backend/src/app.ts`, agregar import (después de línea 10):

```ts
import { meFavoritesRouter } from "./modules/favorites/favorites.routes.js";
```

Y agregar el `app.use` después del de comparaciones (línea 44):

```ts
app.use("/api/v1/me/favorites", meFavoritesRouter);
```

- [ ] **Step 5: Run tests**

Run: `npm -w apps/backend run test -- favorites`
Expected: PASS, 5 tests.

- [ ] **Step 6: Commit**

```bash
git add apps/backend/src/modules/favorites apps/backend/src/app.ts
git commit -m "feat(be): add favorites endpoints with auth"
```

---

### Task 5: `ModelsService.list()` con sort + order

**Files:**
- Modify: `apps/backend/src/modules/models/models.dto.ts`
- Modify: `apps/backend/src/modules/models/models.service.ts`
- Create or Modify: `apps/backend/src/modules/models/models.controller.spec.ts` (extender)

- [ ] **Step 1: Extender schema con sort/order**

En `apps/backend/src/modules/models/models.dto.ts`, agregar al schema:

```ts
import { z } from "zod";

export const listModelsQuerySchema = z.object({
  brand: z.string().optional(),
  segment: z.enum(["SEDAN", "SUV", "HATCHBACK", "PICKUP", "CROSSOVER", "COMMERCIAL"]).optional(),
  year: z.coerce.number().int().optional(),
  transmission: z.enum(["MANUAL", "AUTOMATIC", "CVT", "DCT"]).optional(),
  fuel: z.enum(["BENCINA", "DIESEL", "HYBRID", "ELECTRIC"]).optional(),
  priceMin: z.coerce.number().int().optional(),
  priceMax: z.coerce.number().int().optional(),
  powerMin: z.coerce.number().int().optional(),
  consumptionMax: z.coerce.number().optional(),
  sort: z.enum(["name", "minPrice", "minConsumption"]).default("name"),
  order: z.enum(["asc", "desc"]).default("asc"),
  page: z.coerce.number().int().min(1).default(1),
  pageSize: z.coerce.number().int().min(1).max(50).default(20),
});
```

- [ ] **Step 2: Agregar `minConsumption` y `.sort()` en `ModelsService.list()`**

Reemplazar la función `list()` en `apps/backend/src/modules/models/models.service.ts` por:

```ts
async list(q: z.infer<typeof listModelsQuerySchema>) {
  const where: Prisma.ModelWhereInput = {};
  if (q.brand) where.brandId = q.brand;
  if (q.segment) where.segment = q.segment;

  const vWhere: Prisma.VersionWhereInput = {};
  if (q.transmission) vWhere.transmission = q.transmission;
  if (q.fuel) vWhere.fuel = q.fuel;
  if (q.year !== undefined) vWhere.year = q.year;
  if (q.priceMin !== undefined || q.priceMax !== undefined) {
    vWhere.priceClp = {
      ...(q.priceMin !== undefined ? { gte: q.priceMin } : {}),
      ...(q.priceMax !== undefined ? { lte: q.priceMax } : {}),
    };
  }
  if (q.powerMin !== undefined) vWhere.powerHp = { gte: q.powerMin };
  if (q.consumptionMax !== undefined) vWhere.consumptionCityKmL = { lte: q.consumptionMax };

  if (Object.keys(vWhere).length > 0) where.versions = { some: vWhere };

  const [total, items] = await this.prisma.$transaction([
    this.prisma.model.count({ where }),
    this.prisma.model.findMany({
      where,
      include: { brand: true, versions: { orderBy: { priceClp: "asc" } } },
      skip: (q.page - 1) * q.pageSize,
      take: q.pageSize,
      orderBy: { name: "asc" },
    }),
  ]);

  const enriched = items.map((m) => {
    const prices = m.versions.map((v) => v.priceClp);
    const consumptions = m.versions
      .map((v) => v.consumptionCityKmL)
      .filter((c): c is number => typeof c === "number");
    const minPrice = prices.length ? Math.min(...prices) : null;
    const minConsumption = consumptions.length ? Math.min(...consumptions) : null;
    const firstVersion = m.versions[0];
    const defaultVersion = firstVersion
      ? {
          id: firstVersion.id,
          name: firstVersion.name,
          priceClp: firstVersion.priceClp,
          year: firstVersion.year,
        }
      : null;
    const versions = m.versions.map((v) => ({
      id: v.id,
      name: v.name,
      year: v.year,
      priceClp: v.priceClp,
      transmission: v.transmission,
      fuel: v.fuel,
      engineDisplacementCc: v.engineDisplacementCc,
      powerHp: v.powerHp,
      torqueNm: v.torqueNm,
      consumptionCityKmL: v.consumptionCityKmL,
      consumptionHighwayKmL: v.consumptionHighwayKmL,
    }));
    return {
      id: m.id, brandId: m.brandId, name: m.name, segment: m.segment,
      imageUrl: m.galleryUrls[0] ?? m.imageUrl ?? null,
      galleryUrls: m.galleryUrls, brand: m.brand,
      minPrice, minConsumption, versionCount: m.versions.length,
      defaultVersion,
      versions,
    };
  });

  enriched.sort((a, b) => {
    const dir = q.order === "desc" ? -1 : 1;
    let cmp = 0;
    if (q.sort === "minPrice") {
      cmp = ((a.minPrice ?? Infinity) - (b.minPrice ?? Infinity));
    } else if (q.sort === "minConsumption") {
      cmp = ((a.minConsumption ?? Infinity) - (b.minConsumption ?? Infinity));
    } else {
      cmp = a.name.localeCompare(b.name);
    }
    return cmp * dir;
  });

  return { total, items: enriched, page: q.page, pageSize: q.pageSize };
}
```

- [ ] **Step 3: Verificar que el controller pasa `q` completo**

Abrir `apps/backend/src/modules/models/models.controller.ts`. Confirmar que `list` pasa `req.query` validado al servicio. Si valida contra `listModelsQuerySchema` y pasa `parsed.data`, no se requieren cambios. Si solo pasa `req.query` sin validar, ajustar para usar `listModelsQuerySchema.parse(req.query)`.

- [ ] **Step 4: Tests en `models.controller.spec.ts`**

Verificar si existe el archivo. Si no, crear uno basado en `comparisons.controller.spec.ts`. Agregar tests:

```ts
import { describe, it, expect, beforeEach } from "vitest";
import request from "supertest";
import { createApp } from "../../app.js";
import { setupTestPrisma, resetTestDb } from "../../../__tests__/helpers/db.js";
import { prisma } from "../../infra/prisma.js";

describe("Models endpoints", () => {
  beforeEach(async () => {
    setupTestPrisma();
    await resetTestDb(prisma);
  });

  async function seedModels() {
    const b = await prisma.brand.create({ data: { name: "T" } });
    const a = await prisma.model.create({ data: { brandId: b.id, name: "Alpha", segment: "SEDAN" } });
    const c = await prisma.model.create({ data: { brandId: b.id, name: "Charlie", segment: "SUV" } });
    const z = await prisma.model.create({ data: { brandId: b.id, name: "Zeta", segment: "HATCHBACK" } });
    await prisma.version.create({
      data: { modelId: a.id, name: "A1", year: 2026, priceClp: 20000000, transmission: "AUTOMATIC", fuel: "BENCINA",
        engineDisplacementCc: 1500, powerHp: 120, torqueNm: 145, consumptionCityKmL: 10, consumptionHighwayKmL: 14,
        lengthMm: 4200, widthMm: 1760, heightMm: 1480, weightKg: 1100, trunkLiters: 360, airbagCount: 6,
        hasAbs: true, hasEsp: true, hasCruiseControl: true },
    });
    await prisma.version.create({
      data: { modelId: c.id, name: "C1", year: 2026, priceClp: 15000000, transmission: "AUTOMATIC", fuel: "BENCINA",
        engineDisplacementCc: 2000, powerHp: 150, torqueNm: 200, consumptionCityKmL: 14, consumptionHighwayKmL: 18,
        lengthMm: 4500, widthMm: 1850, heightMm: 1500, weightKg: 1300, trunkLiters: 420, airbagCount: 6,
        hasAbs: true, hasEsp: true, hasCruiseControl: true },
    });
    await prisma.version.create({
      data: { modelId: z.id, name: "Z1", year: 2026, priceClp: 10000000, transmission: "MANUAL", fuel: "BENCINA",
        engineDisplacementCc: 1200, powerHp: 80, torqueNm: 110, consumptionCityKmL: 16, consumptionHighwayKmL: 20,
        lengthMm: 4000, widthMm: 1700, heightMm: 1450, weightKg: 1000, trunkLiters: 320, airbagCount: 4,
        hasAbs: true, hasEsp: false, hasCruiseControl: false },
    });
    return { a, c, z };
  }

  it("GET /models default ordena por nombre asc", async () => {
    await seedModels();
    const res = await request(createApp()).get("/api/v1/models");
    expect(res.status).toBe(200);
    const names = res.body.data.items.map((m: { name: string }) => m.name);
    expect(names).toEqual(["Alpha", "Charlie", "Zeta"]);
  });

  it("GET /models?sort=minPrice&order=asc ordena por menor precio", async () => {
    await seedModels();
    const res = await request(createApp()).get("/api/v1/models?sort=minPrice&order=asc");
    const names = res.body.data.items.map((m: { name: string }) => m.name);
    expect(names).toEqual(["Zeta", "Charlie", "Alpha"]);
  });

  it("GET /models?sort=minPrice&order=desc ordena por mayor precio", async () => {
    await seedModels();
    const res = await request(createApp()).get("/api/v1/models?sort=minPrice&order=desc");
    const names = res.body.data.items.map((m: { name: string }) => m.name);
    expect(names).toEqual(["Alpha", "Charlie", "Zeta"]);
  });

  it("GET /models?sort=minConsumption&order=asc ordena por menor consumo", async () => {
    await seedModels();
    const res = await request(createApp()).get("/api/v1/models?sort=minConsumption&order=asc");
    const names = res.body.data.items.map((m: { name: string }) => m.name);
    expect(names).toEqual(["Alpha", "Charlie", "Zeta"]);
  });

  it("GET /models?transmission=MANUAL filtra correctamente", async () => {
    await seedModels();
    const res = await request(createApp()).get("/api/v1/models?transmission=MANUAL");
    expect(res.body.data.items).toHaveLength(1);
    expect(res.body.data.items[0].name).toBe("Zeta");
  });
});
```

- [ ] **Step 5: Run tests**

Run: `npm -w apps/backend run test -- models`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add apps/backend/src/modules/models
git commit -m "feat(be): catalog sort and order params"
```

---

## Fase 2 — Frontend: FavoritesStore + VehicleCard extendido

### Task 6: `FavoritesStore`

**Files:**
- Create: `apps/frontend/src/app/core/favorites-store.service.ts`
- Create: `apps/frontend/src/app/core/favorites-store.service.spec.ts`

- [ ] **Step 1: Crear `favorites-store.service.ts`**

```ts
import { Injectable, computed, effect, inject, signal } from '@angular/core';
import { ApiService } from './api.service';
import { AuthService } from './auth.service';

@Injectable({ providedIn: 'root' })
export class FavoritesStore {
  private api = inject(ApiService);
  private auth = inject(AuthService);

  private _ids = signal<Set<string>>(new Set());
  readonly ids = computed(() => Array.from(this._ids()));
  readonly count = computed(() => this._ids().size);
  readonly loaded = signal(false);

  constructor() {
    effect(() => {
      const u = this.auth.currentUser();
      if (u) {
        this.load();
      } else {
        this._ids.set(new Set());
        this.loaded.set(false);
      }
    });
  }

  isFavorite(modelId: string): boolean {
    return this._ids().has(modelId);
  }

  async toggle(modelId: string): Promise<void> {
    if (!this.auth.currentUser()) throw new Error('UNAUTHORIZED');
    if (this._ids().has(modelId)) {
      await this.api.delete(`/me/favorites/${modelId}`);
      this._ids.update((s) => {
        const n = new Set(s);
        n.delete(modelId);
        return n;
      });
    } else {
      await this.api.post('/me/favorites', { modelId });
      this._ids.update((s) => {
        const n = new Set(s);
        n.add(modelId);
        return n;
      });
    }
  }

  async load(): Promise<void> {
    try {
      const res = await this.api.get<{ data: { modelIds: string[] } }>('/me/favorites');
      this._ids.set(new Set(res.data.modelIds));
    } finally {
      this.loaded.set(true);
    }
  }
}
```

- [ ] **Step 2: Crear `favorites-store.service.spec.ts`**

```ts
import { TestBed } from '@angular/core/testing';
import { provideHttpClient } from '@angular/common/http';
import { provideHttpClientTesting, HttpTestingController } from '@angular/common/http/testing';
import { signal } from '@angular/core';
import { FavoritesStore } from './favorites-store.service';
import { AuthService, type User } from './auth.service';

class AuthServiceStub {
  private _user = signal<User | null>(null);
  currentUser = this._user.asReadonly();
  setUser(u: User | null) { this._user.set(u); }
}

describe('FavoritesStore', () => {
  let http: HttpTestingController;
  let store: FavoritesStore;
  let authStub: AuthServiceStub;

  beforeEach(() => {
    TestBed.resetTestingModule();
    TestBed.configureTestingModule({
      providers: [
        provideHttpClient(),
        provideHttpClientTesting(),
        { provide: AuthService, useClass: AuthServiceStub },
        FavoritesStore,
      ],
    });
    http = TestBed.inject(HttpTestingController);
    authStub = TestBed.inject(AuthService) as unknown as AuthServiceStub;
    store = TestBed.inject(FavoritesStore);
  });

  afterEach(() => {
    http.verify();
  });

  it('toggle sin user lanza UNAUTHORIZED', async () => {
    await expectAsync(store.toggle('m1')).toBeRejectedWithError('UNAUTHORIZED');
  });

  it('toggle con user llama POST y agrega al set', async () => {
    authStub.setUser({ id: 'u1', email: 'u@test.cl', name: 'U' });
    // effect() dispara load() — flushear el GET primero
    await Promise.resolve();
    const getReq = http.expectOne((r) => r.url.includes('/me/favorites'));
    getReq.flush({ data: { modelIds: [] } });

    const p = store.toggle('m1');
    const postReq = http.expectOne((r) => r.url.includes('/me/favorites') && r.method === 'POST');
    postReq.flush({ data: { modelId: 'm1', created: true } });
    await p;

    expect(store.isFavorite('m1')).toBe(true);
    expect(store.count()).toBe(1);
  });

  it('toggle cuando ya es favorito llama DELETE y remueve', async () => {
    authStub.setUser({ id: 'u1', email: 'u@test.cl', name: 'U' });
    await Promise.resolve();
    http.expectOne((r) => r.url.includes('/me/favorites')).flush({ data: { modelIds: ['m1'] } });

    const p = store.toggle('m1');
    const delReq = http.expectOne((r) => r.url.includes('/me/favorites/m1') && r.method === 'DELETE');
    delReq.flush({ data: { removed: true } });
    await p;

    expect(store.isFavorite('m1')).toBe(false);
    expect(store.count()).toBe(0);
  });

  it('logout limpia el set', async () => {
    authStub.setUser({ id: 'u1', email: 'u@test.cl', name: 'U' });
    await Promise.resolve();
    http.expectOne((r) => r.url.includes('/me/favorites')).flush({ data: { modelIds: ['m1', 'm2'] } });
    expect(store.count()).toBe(2);

    authStub.setUser(null);
    await Promise.resolve();
    expect(store.count()).toBe(0);
    expect(store.isFavorite('m1')).toBe(false);
  });
});
```

- [ ] **Step 3: Run tests**

Run: `npm -w apps/frontend run test -- favorites-store`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add apps/frontend/src/app/core/favorites-store.service.ts apps/frontend/src/app/core/favorites-store.service.spec.ts
git commit -m "feat(fe): add FavoritesStore with auth-reactive effect"
```

---

### Task 7: `VehicleCardComponent` — botón corazón

**Files:**
- Modify: `apps/frontend/src/app/shared/ui/vehicle-card.component.ts`
- Modify: `apps/frontend/src/app/shared/ui/vehicle-card.component.html`
- Modify: `apps/frontend/src/app/shared/ui/vehicle-card.component.spec.ts`

- [ ] **Step 1: Agregar inputs/outputs al component TS**

En `apps/frontend/src/app/shared/ui/vehicle-card.component.ts`, agregar:

```ts
import { AuthService } from '../../core/auth.service';
```

En la clase `VehicleCardComponent`, agregar:

```ts
private auth = inject(AuthService);

readonly isFavorite = input<boolean>(false);
readonly favoriteToggled = output<void>();

readonly canFavorite = computed(() => this.auth.currentUser() !== null);

onFavorite(event: Event): void {
  event.stopPropagation();
  if (!this.canFavorite()) return;
  this.favoriteToggled.emit();
}
```

- [ ] **Step 2: Agregar botón corazón al HTML**

En `apps/frontend/src/app/shared/ui/vehicle-card.component.html`, agregar al inicio del `<article>` (antes de los demás hijos), junto al featured pill:

```html
@if (canFavorite()) {
  <button
    type="button"
    (click)="onFavorite($event)"
    [attr.aria-label]="(isFavorite() ? 'Quitar ' : 'Marcar ') + model().brand.name + ' ' + model().name + ' como favorito'"
    [attr.aria-pressed]="isFavorite() ? 'true' : 'false'"
    [attr.data-testid]="'favorite-' + model().id"
    [attr.data-favorite]="isFavorite() ? 'true' : 'false'"
    class="absolute top-2 right-2 z-20 grid h-8 w-8 place-items-center rounded-full bg-surface/90 hover:bg-surface transition-colors"
  >
    <span
      class="material-symbols-outlined text-lg"
      [class.text-error]="isFavorite()"
      [class.text-ink-muted]="!isFavorite()"
      [style.font-variation-settings]="isFavorite() ? \"'FILL' 1\" : \"'FILL' 0\""
    >favorite</span>
  </button>
}
```

- [ ] **Step 3: Ajustar tests existentes**

Verificar que `vehicle-card.component.spec.ts` no asuma que no hay botón favorito. Buscar `data-testid="vehicle-card-` y agregar un stub para `AuthService`:

En el `beforeEach` del archivo spec, agregar provider:

```ts
import { AuthService } from '../../core/auth.service';
class AuthServiceStub {
  currentUser = signal<User | null>({ id: 'u1', email: 'u@test.cl', name: 'U' });
}
// ...
providers: [
  // ... existing
  { provide: AuthService, useClass: AuthServiceStub },
],
```

(Si el spec ya provee `AuthService`, ajustar para usar un stub que devuelva user válido.)

- [ ] **Step 4: Agregar tests nuevos al spec**

```ts
it('botón corazón filled cuando isFavorite=true', () => {
  const fixture = TestBed.createComponent(VehicleCardComponent);
  fixture.componentRef.setInput('model', testModel);
  fixture.componentRef.setInput('isFavorite', true);
  fixture.detectChanges();
  const btn = fixture.nativeElement.querySelector(
    `[data-testid="favorite-${testModel.id}"]`,
  );
  expect(btn).not.toBeNull();
  expect(btn.getAttribute('data-favorite')).toBe('true');
});

it('botón corazón outline cuando isFavorite=false', () => {
  const fixture = TestBed.createComponent(VehicleCardComponent);
  fixture.componentRef.setInput('model', testModel);
  fixture.componentRef.setInput('isFavorite', false);
  fixture.detectChanges();
  const btn = fixture.nativeElement.querySelector(
    `[data-testid="favorite-${testModel.id}"]`,
  );
  expect(btn.getAttribute('data-favorite')).toBe('false');
});

it('click emite favoriteToggled', () => {
  const fixture = TestBed.createComponent(VehicleCardComponent);
  fixture.componentRef.setInput('model', testModel);
  let emitted = false;
  fixture.componentInstance.favoriteToggled.subscribe(() => (emitted = true));
  fixture.detectChanges();
  const btn: HTMLButtonElement = fixture.nativeElement.querySelector(
    `[data-testid="favorite-${testModel.id}"]`,
  );
  btn.click();
  expect(emitted).toBe(true);
});
```

- [ ] **Step 5: Run tests**

Run: `npm -w apps/frontend run test -- vehicle-card`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add apps/frontend/src/app/shared/ui/vehicle-card.component.ts apps/frontend/src/app/shared/ui/vehicle-card.component.html apps/frontend/src/app/shared/ui/vehicle-card.component.spec.ts
git commit -m "feat(fe): favorite toggle button on vehicle card"
```

---

## Fase 3 — Frontend: Catálogo extendido + landing

### Task 8: `CatalogComponent` — filtros extendidos + sort/order

**Files:**
- Modify: `apps/frontend/src/app/features/catalog/catalog.component.ts`
- Modify: `apps/frontend/src/app/features/catalog/catalog.component.html`
- Modify: `apps/frontend/src/app/features/catalog/catalog.component.spec.ts`

- [ ] **Step 1: Extender `CatalogFilters` y agregar brands/sort/order en TS**

Reemplazar `apps/frontend/src/app/features/catalog/catalog.component.ts` por:

```ts
import { Component, computed, inject, signal } from '@angular/core';
import { RouterLink } from '@angular/router';
import { ApiService, QueryParams } from '../../core/api.service';
import { CompareStore } from '../../core/compare-store.service';
import {
  VehicleCardComponent,
  VehicleCardInput,
} from '../../shared/ui/vehicle-card.component';
import { VehicleVersion } from '../../core/types/vehicle';

type Segment =
  | 'SEDAN'
  | 'SUV'
  | 'HATCHBACK'
  | 'PICKUP'
  | 'CROSSOVER'
  | 'COMMERCIAL';

type Transmission = 'MANUAL' | 'AUTOMATIC' | 'CVT' | 'DCT';
type Fuel = 'BENCINA' | 'DIESEL' | 'HYBRID' | 'ELECTRIC';
type Sort = 'name' | 'minPrice' | 'minConsumption';
type Order = 'asc' | 'desc';

export interface CatalogFilters {
  brand?: string;
  segment?: Segment;
  priceMin?: number;
  priceMax?: number;
  transmission?: Transmission;
  fuel?: Fuel;
  year?: number;
  powerMin?: number;
  consumptionMax?: number;
  sort?: Sort;
  order?: Order;
}

const FEATURED_MODEL_NAMES = new Set(['Corolla', 'Tucson', 'CX-5']);
const YEARS: ReadonlyArray<number> = [2024, 2025, 2026, 2027];

@Component({
  selector: 'app-catalog',
  templateUrl: './catalog.component.html',
  styleUrl: './catalog.component.css',
  imports: [RouterLink, VehicleCardComponent],
})
export class CatalogComponent {
  private api = inject(ApiService);
  private compare = inject(CompareStore);

  readonly segments: ReadonlyArray<{ value: Segment; label: string }> = [
    { value: 'SEDAN', label: 'Sedán' },
    { value: 'SUV', label: 'SUV' },
    { value: 'HATCHBACK', label: 'Hatchback' },
    { value: 'PICKUP', label: 'Pickup' },
    { value: 'CROSSOVER', label: 'Crossover' },
    { value: 'COMMERCIAL', label: 'Comercial' },
  ];

  readonly transmissions: ReadonlyArray<{ value: Transmission; label: string }> = [
    { value: 'MANUAL', label: 'Manual' },
    { value: 'AUTOMATIC', label: 'Automática' },
    { value: 'CVT', label: 'CVT' },
    { value: 'DCT', label: 'DCT' },
  ];

  readonly fuels: ReadonlyArray<{ value: Fuel; label: string }> = [
    { value: 'BENCINA', label: 'Bencina' },
    { value: 'DIESEL', label: 'Diésel' },
    { value: 'HYBRID', label: 'Híbrido' },
    { value: 'ELECTRIC', label: 'Eléctrico' },
  ];

  readonly sortOptions: ReadonlyArray<{ value: Sort; label: string; tip?: string }> = [
    { value: 'name', label: 'Nombre' },
    { value: 'minPrice', label: 'Precio' },
    { value: 'minConsumption', label: 'Rendimiento', tip: 'Rendimiento = menor consumo de combustible en ciudad (km/L).' },
  ];

  readonly years = YEARS;

  readonly brands = signal<Array<{ id: string; name: string }>>([]);

  filters = signal<CatalogFilters>({ sort: 'name', order: 'asc' });
  items = signal<VehicleCardInput[]>([]);
  total = signal(0);
  loading = signal(false);

  readonly selectedVersions = signal<Record<string, string>>({});

  readonly selectedIds = this.compare.ids;
  readonly selectionCount = computed(() => this.selectedIds().length);
  readonly maxReached = computed(() => this.selectionCount() >= 3);

  isAdded(item: VehicleCardInput): boolean {
    const id = this.selectedVersionId(item);
    return id ? this.selectedIds().includes(id) : false;
  }

  selectedVersionId(item: VehicleCardInput): string | null {
    const override = this.selectedVersions()[item.id];
    if (override) return override;
    return item.versions[0]?.id ?? item.defaultVersion?.id ?? null;
  }

  isFeatured(name: string): boolean {
    return FEATURED_MODEL_NAMES.has(name);
  }

  readonly initialLoad: Promise<void>;

  constructor() {
    this.initialLoad = Promise.all([this.loadBrands(), this.load()]);
  }

  private async loadBrands(): Promise<void> {
    try {
      const res = await this.api.get<{ data: Array<{ id: string; name: string }> }>('/brands');
      this.brands.set(res.data);
    } catch {
      /* ignore */
    }
  }

  async load(): Promise<void> {
    this.loading.set(true);
    try {
      const res = await this.api.get<{
        data: { items: VehicleCardInput[]; total: number };
      }>('/models', this.cleanParams(this.filters()));
      this.items.set(res.data.items);
      this.total.set(res.data.total);
    } finally {
      this.loading.set(false);
    }
  }

  updateFilter(patch: Partial<CatalogFilters>): Promise<void> {
    this.filters.update((f) => ({ ...f, ...patch }));
    return this.load();
  }

  addToCompare(item: VehicleCardInput): void {
    const versionId = this.selectedVersionId(item);
    if (!versionId) return;
    if (this.selectedIds().includes(versionId)) {
      this.compare.remove(versionId);
    } else {
      this.compare.add(versionId);
    }
  }

  onVersionSelected(item: VehicleCardInput, v: VehicleVersion): void {
    this.selectedVersions.update((m) => ({ ...m, [item.id]: v.id }));
  }

  clearSelection(): void {
    this.compare.clear();
  }

  async clearFilters(): Promise<void> {
    this.filters.set({ sort: 'name', order: 'asc' });
    await this.load();
  }

  formatPrice(value: number | null | undefined): string {
    if (value === null || value === undefined) return '';
    return new Intl.NumberFormat('es-CL').format(value);
  }

  private cleanParams(f: CatalogFilters): QueryParams {
    const out: QueryParams = {};
    for (const [k, v] of Object.entries(f)) {
      if (v !== undefined && v !== null && v !== '') {
        out[k] = v as QueryParams[string];
      }
    }
    return out;
  }
}
```

- [ ] **Step 2: Actualizar `catalog.component.html` con nuevos filtros y orden**

Reemplazar `apps/frontend/src/app/features/catalog/catalog.component.html` por:

```html
<section>
  <div class="border-b border-border bg-surface-muted">
    <div class="mx-auto max-w-7xl px-4 py-6 flex items-end justify-between">
      <div>
        <p class="text-xs uppercase tracking-wider text-brand-700 font-bold">
          Catálogo
        </p>
        <h1 class="text-2xl md:text-3xl font-bold text-ink tracking-tight mt-1">
          Explorá el mercado chileno
        </h1>
      </div>
      <p class="hidden md:block text-sm text-ink-muted">
        Filtrá por marca, segmento, transmisión, combustible y rango de precio.
      </p>
    </div>
  </div>

  <div class="mx-auto max-w-7xl px-4 py-6 md:py-8 flex flex-col md:flex-row gap-6">
    <aside class="w-full md:w-72 flex-shrink-0">
      <div class="md:sticky md:top-24 bg-surface border border-border rounded-xl shadow-sm p-5 space-y-5">
        <header class="flex items-center justify-between">
          <h2 class="font-bold text-ink flex items-center gap-2 text-lg">
            <span class="material-symbols-outlined">filter_list</span>
            Filtros
          </h2>
          <button
            type="button"
            class="text-xs font-semibold text-ink-muted underline hover:text-brand-700"
            (click)="clearFilters()"
            data-testid="filters-clear"
          >Limpiar</button>
        </header>

        <div>
          <label class="block text-sm font-semibold text-ink-muted mb-2">Segmento</label>
          <div class="space-y-2">
            <label class="flex items-center gap-2 text-sm cursor-pointer text-ink">
              <input type="radio" name="segment" class="rounded text-brand-600 focus:ring-brand-500 border-border"
                [checked]="!filters().segment"
                (change)="updateFilter({ segment: undefined })" />
              Cualquiera
            </label>
            @for (s of segments; track s.value) {
              <label class="flex items-center gap-2 text-sm cursor-pointer text-ink">
                <input type="radio" name="segment" class="rounded text-brand-600 focus:ring-brand-500 border-border"
                  [checked]="filters().segment === s.value"
                  (change)="updateFilter({ segment: s.value })" />
                {{ s.label }}
              </label>
            }
          </div>
        </div>

        <div>
          <label class="block text-sm font-semibold text-ink-muted mb-2" for="filter-brand">Marca</label>
          <select id="filter-brand"
            class="w-full rounded border border-border bg-surface px-2 py-1 text-sm text-ink focus:outline-none focus:ring-2 focus:ring-brand-500"
            [value]="filters().brand ?? ''"
            (change)="updateFilter({ brand: $any($event.target).value || undefined })"
            data-testid="filter-brand">
            <option value="">Cualquiera</option>
            @for (b of brands(); track b.id) {
              <option [value]="b.id">{{ b.name }}</option>
            }
          </select>
        </div>

        <div>
          <label class="block text-sm font-semibold text-ink-muted mb-2">Transmisión</label>
          <div class="space-y-2">
            <label class="flex items-center gap-2 text-sm cursor-pointer text-ink">
              <input type="radio" name="transmission" class="rounded text-brand-600 focus:ring-brand-500 border-border"
                [checked]="!filters().transmission"
                (change)="updateFilter({ transmission: undefined })" />
              Cualquiera
            </label>
            @for (t of transmissions; track t.value) {
              <label class="flex items-center gap-2 text-sm cursor-pointer text-ink">
                <input type="radio" name="transmission" class="rounded text-brand-600 focus:ring-brand-500 border-border"
                  [checked]="filters().transmission === t.value"
                  (change)="updateFilter({ transmission: t.value })" />
                {{ t.label }}
              </label>
            }
          </div>
        </div>

        <div>
          <label class="block text-sm font-semibold text-ink-muted mb-2">Combustible</label>
          <div class="space-y-2">
            <label class="flex items-center gap-2 text-sm cursor-pointer text-ink">
              <input type="radio" name="fuel" class="rounded text-brand-600 focus:ring-brand-500 border-border"
                [checked]="!filters().fuel"
                (change)="updateFilter({ fuel: undefined })" />
              Cualquiera
            </label>
            @for (f of fuels; track f.value) {
              <label class="flex items-center gap-2 text-sm cursor-pointer text-ink">
                <input type="radio" name="fuel" class="rounded text-brand-600 focus:ring-brand-500 border-border"
                  [checked]="filters().fuel === f.value"
                  (change)="updateFilter({ fuel: f.value })" />
                {{ f.label }}
              </label>
            }
          </div>
        </div>

        <div>
          <label class="block text-sm font-semibold text-ink-muted mb-2" for="filter-year">Año</label>
          <select id="filter-year"
            class="w-full rounded border border-border bg-surface px-2 py-1 text-sm text-ink focus:outline-none focus:ring-2 focus:ring-brand-500"
            [value]="filters().year ?? ''"
            (change)="updateFilter({ year: +$any($event.target).value || undefined })"
            data-testid="filter-year">
            <option value="">Cualquiera</option>
            @for (y of years; track y) {
              <option [value]="y">{{ y }}</option>
            }
          </select>
        </div>

        <div>
          <label class="block text-sm font-semibold text-ink-muted mb-2" for="filter-priceMin">Precio mínimo (CLP)</label>
          <input id="filter-priceMin" type="number" min="0" step="100000" placeholder="0"
            class="w-full rounded border border-border bg-surface px-2 py-1 text-sm text-ink focus:outline-none focus:ring-2 focus:ring-brand-500"
            (change)="updateFilter({ priceMin: +$any($event.target).value || undefined })"
            [value]="filters().priceMin ?? ''" />
        </div>

        <div>
          <label class="block text-sm font-semibold text-ink-muted mb-2" for="filter-priceMax">Precio máximo (CLP)</label>
          <input id="filter-priceMax" type="number" min="0" step="100000" placeholder="30.000.000"
            class="w-full rounded border border-border bg-surface px-2 py-1 text-sm text-ink focus:outline-none focus:ring-2 focus:ring-brand-500"
            (change)="updateFilter({ priceMax: +$any($event.target).value || undefined })"
            [value]="filters().priceMax ?? ''" />
        </div>

        <div>
          <label class="block text-sm font-semibold text-ink-muted mb-2" for="filter-powerMin">Potencia mínima (hp)</label>
          <input id="filter-powerMin" type="number" min="0" step="10" placeholder="80"
            class="w-full rounded border border-border bg-surface px-2 py-1 text-sm text-ink focus:outline-none focus:ring-2 focus:ring-brand-500"
            (change)="updateFilter({ powerMin: +$any($event.target).value || undefined })"
            [value]="filters().powerMin ?? ''"
            data-testid="filter-powerMin" />
        </div>

        <div>
          <label class="block text-sm font-semibold text-ink-muted mb-2" for="filter-consumptionMax">Consumo máx. ciudad (km/L)</label>
          <input id="filter-consumptionMax" type="number" min="0" step="0.5" placeholder="20"
            class="w-full rounded border border-border bg-surface px-2 py-1 text-sm text-ink focus:outline-none focus:ring-2 focus:ring-brand-500"
            (change)="updateFilter({ consumptionMax: +$any($event.target).value || undefined })"
            [value]="filters().consumptionMax ?? ''"
            data-testid="filter-consumptionMax" />
        </div>

        <p class="text-xs text-ink-muted">
          <span class="font-bold text-ink" data-testid="catalog-total">{{ total() }}</span>
          modelos disponibles
        </p>
      </div>
    </aside>

    <main class="flex-1">
      <div class="mb-4 flex items-center gap-3 justify-end" data-testid="sort-bar">
        <label class="text-xs text-ink-muted" for="sort-select">Ordenar por</label>
        <select id="sort-select"
          class="rounded border border-border bg-surface px-2 py-1 text-sm text-ink focus:outline-none focus:ring-2 focus:ring-brand-500"
          [value]="filters().sort ?? 'name'"
          (change)="updateFilter({ sort: $any($event.target).value })"
          [attr.title]="sortTip()"
          data-testid="sort-select">
          @for (opt of sortOptions; track opt.value) {
            <option [value]="opt.value" [attr.title]="opt.tip ?? ''">{{ opt.label }}</option>
          }
        </select>
        <div class="flex items-center border border-border rounded overflow-hidden" role="group" aria-label="Dirección">
          <button type="button"
            (click)="updateFilter({ order: 'asc' })"
            [attr.aria-pressed]="(filters().order ?? 'asc') === 'asc'"
            [class.bg-brand-600]="(filters().order ?? 'asc') === 'asc'"
            [class.text-white]="(filters().order ?? 'asc') === 'asc'"
            [class.text-ink-muted]="(filters().order ?? 'asc') !== 'asc'"
            class="grid place-items-center w-8 h-8 hover:bg-surface-muted"
            data-testid="sort-asc"
            aria-label="Ascendente">
            <span class="material-symbols-outlined text-base">arrow_upward</span>
          </button>
          <button type="button"
            (click)="updateFilter({ order: 'desc' })"
            [attr.aria-pressed]="filters().order === 'desc'"
            [class.bg-brand-600]="filters().order === 'desc'"
            [class.text-white]="filters().order === 'desc'"
            [class.text-ink-muted]="filters().order !== 'desc'"
            class="grid place-items-center w-8 h-8 hover:bg-surface-muted"
            data-testid="sort-desc"
            aria-label="Descendente">
            <span class="material-symbols-outlined text-base">arrow_downward</span>
          </button>
        </div>
      </div>

      @if (loading()) {
        <p class="text-sm text-brand-600" data-testid="loading">Cargando…</p>
      }

      @if (items().length === 0 && !loading()) {
        <div class="rounded-xl border border-border bg-surface p-12 text-center shadow-sm" data-testid="empty">
          <p class="font-bold text-ink mb-2">No se encontraron modelos</p>
          <p class="text-sm text-ink-muted">Probá ajustar los filtros o limpiarlos.</p>
          <button type="button"
            class="mt-4 inline-block rounded bg-brand-600 px-4 py-2 text-sm text-white hover:bg-brand-700"
            (click)="clearFilters()">Limpiar filtros</button>
        </div>
      }

      <ul class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4" data-testid="catalog-grid">
        @for (m of items(); track m.id) {
          <li>
            <app-vehicle-card
              [model]="m"
              [featured]="isFeatured(m.name)"
              [added]="isAdded(m)"
              [maxReached]="maxReached()"
              [selectedVersionId]="selectedVersionId(m)"
              (compareTapped)="addToCompare(m)"
              (versionSelected)="onVersionSelected(m, $event)"
            />
          </li>
        }
      </ul>

      @if (selectionCount() > 0) {
        <div class="sticky bottom-4 mt-6 z-30 rounded-2xl bg-ink text-surface shadow-e2 p-4 flex items-center justify-between gap-4 border border-brand-600" data-testid="selection-bar">
          <div class="flex items-center gap-3">
            <span class="grid h-8 w-8 place-items-center rounded-full bg-brand-600 text-white text-xs font-bold">{{ selectionCount() }}</span>
            <span class="text-sm">
              {{ selectionCount() === 1 ? '1 versión seleccionada' : selectionCount() + ' versiones seleccionadas' }}
              @if (!maxReached()) {
                <span class="text-ink-subtle"> · podés agregar hasta 3</span>
              }
            </span>
          </div>
          <div class="flex items-center gap-2">
            <button type="button" (click)="clearSelection()"
              [attr.aria-label]="'Limpiar selección de ' + selectionCount() + ' versiones'"
              data-testid="selection-bar-clear"
              class="rounded-full bg-ink text-surface border border-surface-muted hover:bg-surface-muted text-xs font-bold px-3 py-2 transition-colors flex items-center gap-1.5">
              <span class="material-symbols-outlined text-base">delete_sweep</span>
              Limpiar
            </button>
            <a routerLink="/compare"
              class="rounded-full bg-brand-600 text-white text-sm font-bold px-5 py-2 hover:bg-brand-700 transition-colors flex items-center gap-2"
              data-testid="selection-bar-compare">
              <span class="material-symbols-outlined text-base">compare_arrows</span>
              Ver comparación
            </a>
          </div>
        </div>
      }
    </main>
  </div>
</section>
```

- [ ] **Step 3: Agregar helper `sortTip` al TS**

En `apps/frontend/src/app/features/catalog/catalog.component.ts`, agregar método:

```ts
sortTip(): string {
  const opt = this.sortOptions.find((o) => o.value === (this.filters().sort ?? 'name'));
  return opt?.tip ?? '';
}
```

- [ ] **Step 4: Actualizar tests existentes**

Los tests existentes de `catalog.component.spec.ts` ya hacen flush al `/api/v1/models`. Hay que **ajustar** porque ahora `CatalogComponent.constructor` también llama `loadBrands()` (segundo HTTP call). Actualizar `beforeEach` y los tests que asumen un único flush.

Patrón de ajuste para cada test:

```ts
const fixture = TestBed.createComponent(CatalogComponent);
fixture.detectChanges();
// flush brands + models (en cualquier orden)
const brandsReq = http.expectOne((r) => r.url.includes('/api/v1/brands'));
brandsReq.flush({ data: [] });
const modelsReq = http.expectOne((r) => r.url.includes('/api/v1/models'));
modelsReq.flush({ data: { total: 0, items: [], page: 1, pageSize: 20 } });
await fixture.componentInstance.initialLoad;
fixture.detectChanges();
```

Helper para limpiar:

```ts
function flushInitial(fixture: TestBed['createComponent'] extends () => infer F ? F extends { componentInstance: { initialLoad: Promise<void> } } ? any : never : never) {
  const brandsReq = http.expectOne((r) => r.url.includes('/api/v1/brands'));
  brandsReq.flush({ data: [] });
  const modelsReq = http.expectOne((r) => r.url.includes('/api/v1/models'));
  modelsReq.flush({ data: { total: 0, items: [], page: 1, pageSize: 20 } });
  return fixture.componentInstance.initialLoad;
}
```

Usar en cada test después del `TestBed.createComponent(CatalogComponent)`.

- [ ] **Step 5: Agregar tests nuevos**

```ts
it('aplica filtro transmission=AUTOMATIC y se ve en request', async () => {
  const fixture = TestBed.createComponent(CatalogComponent);
  fixture.detectChanges();
  http.expectOne((r) => r.url.includes('/api/v1/brands')).flush({ data: [] });
  http.expectOne((r) => r.url.includes('/api/v1/models')).flush({ data: { total: 0, items: [], page: 1, pageSize: 20 } });
  await fixture.componentInstance.initialLoad;

  const p = fixture.componentInstance.updateFilter({ transmission: 'AUTOMATIC' });
  const req = http.expectOne((r) => r.url.includes('/api/v1/models') && r.params.get('transmission') === 'AUTOMATIC');
  req.flush({ data: { total: 0, items: [], page: 1, pageSize: 20 } });
  await p;
});

it('cambia sort a minPrice y se ve en request', async () => {
  const fixture = TestBed.createComponent(CatalogComponent);
  fixture.detectChanges();
  http.expectOne((r) => r.url.includes('/api/v1/brands')).flush({ data: [] });
  http.expectOne((r) => r.url.includes('/api/v1/models')).flush({ data: { total: 0, items: [], page: 1, pageSize: 20 } });
  await fixture.componentInstance.initialLoad;

  const p = fixture.componentInstance.updateFilter({ sort: 'minPrice' });
  const req = http.expectOne((r) => r.url.includes('/api/v1/models') && r.params.get('sort') === 'minPrice');
  req.flush({ data: { total: 0, items: [], page: 1, pageSize: 20 } });
  await p;
});

it('clearFilters resetea sort y order a defaults', async () => {
  const fixture = TestBed.createComponent(CatalogComponent);
  fixture.detectChanges();
  http.expectOne((r) => r.url.includes('/api/v1/brands')).flush({ data: [] });
  http.expectOne((r) => r.url.includes('/api/v1/models')).flush({ data: { total: 0, items: [], page: 1, pageSize: 20 } });
  await fixture.componentInstance.initialLoad;

  // aplicar sort custom
  const p1 = fixture.componentInstance.updateFilter({ sort: 'minPrice', order: 'desc' });
  http.expectOne((r) => r.url.includes('/api/v1/models')).flush({ data: { total: 0, items: [], page: 1, pageSize: 20 } });
  await p1;

  // clear
  const p2 = fixture.componentInstance.clearFilters();
  const req = http.expectOne((r) => r.url.includes('/api/v1/models') && r.params.get('sort') === 'name' && r.params.get('order') === 'asc');
  req.flush({ data: { total: 0, items: [], page: 1, pageSize: 20 } });
  await p2;
});
```

- [ ] **Step 6: Run tests**

Run: `npm -w apps/frontend run test -- catalog`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add apps/frontend/src/app/features/catalog
git commit -m "feat(fe): catalog extended filters and sort/order"
```

---

### Task 9: `LandingComponent` — cablear favoritos

**Files:**
- Modify: `apps/frontend/src/app/features/landing/landing.component.ts`
- Modify: `apps/frontend/src/app/features/landing/landing.component.html`
- Modify: `apps/frontend/src/app/features/landing/landing.component.spec.ts`

- [ ] **Step 1: Agregar FavoritesStore al TS**

En `apps/frontend/src/app/features/landing/landing.component.ts`, agregar:

```ts
import { FavoritesStore } from '../../core/favorites-store.service';
```

Y en la clase:

```ts
private favorites = inject(FavoritesStore);

onFavoriteToggle(): void {
  // El card ya emitió favoriteToggled; la lógica de toggle vive en el store vía parent.
  // Aquí solo necesitamos exponer el método. La pantalla Favoritos lo cablea distinto.
}
```

En realidad, el handler debe conocer el modelo. Mejor hacer que el card emita con el modelo (no solo el evento) — ajustar Task 7 si es necesario.

En lugar de cambiar el card, agregar al template `(favoriteToggled)="favorites.toggle(m.id)"`:

```html
<app-vehicle-card
  [model]="m"
  [featured]="true"
  [selectedVersionId]="m.versions[0]?.id ?? null"
  [isFavorite]="favorites.isFavorite(m.id)"
  (favoriteToggled)="favorites.toggle(m.id)"
/>
```

Esto requiere `favorites` como público en la clase:

```ts
readonly favorites = inject(FavoritesStore);
```

(Quitar `private`.)

- [ ] **Step 2: Modificar el HTML**

En `apps/frontend/src/app/features/landing/landing.component.html`, en la sección del grid featured (línea ~130), cambiar `<app-vehicle-card>` por:

```html
<app-vehicle-card
  [model]="m"
  [featured]="true"
  [selectedVersionId]="m.versions[0]?.id ?? null"
  [isFavorite]="favorites.isFavorite(m.id)"
  (favoriteToggled)="favorites.toggle(m.id)"
/>
```

- [ ] **Step 3: Ajustar tests existentes**

Los tests existentes del landing no deben romperse (siguen verificando hero, value cards, stats, featured-grid, error). El botón favorito aparece pero solo si hay sesión. Agregar un stub para `AuthService` + `FavoritesStore`:

```ts
import { signal } from '@angular/core';
import { AuthService, type User } from '../../core/auth.service';
import { FavoritesStore } from '../../core/favorites-store.service';

class AuthServiceStub {
  currentUser = signal<User | null>(null);
}

class FavoritesStoreStub {
  isFavorite(): boolean { return false; }
  async toggle(): Promise<void> { /* noop */ }
  load(): Promise<void> { return Promise.resolve(); }
}
```

Agregar providers:

```ts
{ provide: AuthService, useClass: AuthServiceStub },
{ provide: FavoritesStore, useClass: FavoritesStoreStub },
```

- [ ] **Step 4: Commit**

```bash
git add apps/frontend/src/app/features/landing
git commit -m "feat(fe): wire favorites on landing cards"
```

---

## Fase 4 — Frontend: Comparar (dedup, info icon, carrusel)

### Task 10: `CompareComponent` — dedup handler

**Files:**
- Modify: `apps/frontend/src/app/features/compare/compare.component.ts`
- Modify: `apps/frontend/src/app/features/compare/compare.component.html`
- Modify: `apps/frontend/src/app/features/compare/compare.component.spec.ts`

- [ ] **Step 1: Modificar `saveComparison()` para capturar 409**

En `apps/frontend/src/app/features/compare/compare.component.ts`, reemplazar `saveComparison()`:

```ts
async saveComparison(): Promise<void> {
  const ids = this.compareStore.ids();
  if (ids.length === 0) return;
  this.saving.set(true);
  try {
    const res = await this.api.post<{ data: { slug: string } }>(
      '/me/comparisons',
      { versionIds: ids },
    );
    this.savedSlug.set(res.data.slug);
    this.duplicateSlug.set(null);
  } catch (e) {
    const err = e as { status?: number; error?: { code?: string; slug?: string; message?: string } };
    if (err?.status === 409 && err.error?.code === 'COMPARISON_DUPLICATE' && err.error.slug) {
      this.duplicateSlug.set(err.error.slug);
      this.savedSlug.set(err.error.slug);
    } else {
      throw e;
    }
  } finally {
    this.saving.set(false);
  }
}
```

Agregar signal:

```ts
duplicateSlug = signal<string | null>(null);
```

- [ ] **Step 2: Modificar el HTML para mostrar mensaje de duplicado**

En `apps/frontend/src/app/features/compare/compare.component.html`, reemplazar el bloque del banner de guardar (líneas 243-273) por:

```html
<div class="flex flex-wrap items-center justify-between gap-2 rounded-lg bg-brand-50 border border-brand-200 px-4 py-3 text-sm text-brand-900">
  @if (user() && !savedSlug()) {
    <button
      type="button"
      class="rounded-full bg-brand-600 text-white px-4 py-1.5 text-sm font-bold hover:bg-brand-700 transition-colors disabled:opacity-60"
      [disabled]="saving()"
      (click)="saveComparison()"
    >
      {{ saving() ? 'Guardando…' : 'Guardar comparación' }}
    </button>
  } @else if (!user()) {
    <span>Crea una cuenta para guardar esta comparación.</span>
    <a routerLink="/register"
      class="rounded-full bg-brand-600 text-white px-4 py-1.5 text-sm font-bold hover:bg-brand-700 transition-colors">
      Crear cuenta
    </a>
  }
  @if (savedSlug()) {
    <span data-testid="saved-link" class="ml-auto">
      @if (duplicateSlug()) {
        Ya tenés esta comparación guardada.
      }
      <a [routerLink]="['/compare']" [queryParams]="{ slug: savedSlug() }"
        class="text-brand-700 underline">Ver enlace público</a>
    </span>
  }
</div>
```

- [ ] **Step 3: Ajustar tests existentes**

En `compare.component.spec.ts`, no hay un test que verifique 409 actualmente — los tests pasan slug vacío o mocked. No requiere cambios.

- [ ] **Step 4: Agregar test nuevo**

```ts
it('saveComparison con 409 muestra "Ya guardada" + link al slug', async () => {
  store.hydrateFromUrl('a,b');
  const fixture = TestBed.createComponent(CompareComponent);
  await fixture.componentInstance.ready;
  http.expectOne((r) => r.url.includes('/api/v1/compare')).flush({
    data: { versions: [{ id: 'a' }, { id: 'b' }], diffHighlights: {} },
  });
  fixture.detectChanges();

  // Save request
  const p = fixture.componentInstance.saveComparison();
  const saveReq = http.expectOne((r) => r.url.includes('/api/v1/me/comparisons'));
  saveReq.flush(
    { data: null, error: { code: 'COMPARISON_DUPLICATE', slug: 'abc12345', message: 'dup' } },
    { status: 409, statusText: 'Conflict' },
  );
  await p;
  fixture.detectChanges();

  const banner = fixture.nativeElement.querySelector('[data-testid="saved-link"]');
  expect(banner).not.toBeNull();
  expect(banner.textContent).toContain('Ya tenés esta comparación guardada');
});
```

- [ ] **Step 5: Run tests**

Run: `npm -w apps/frontend run test -- compare`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add apps/frontend/src/app/features/compare
git commit -m "feat(fe): handle duplicate comparison save with 409"
```

---

### Task 11: `CompareComponent` — info icon (mover disclaimer)

**Files:**
- Modify: `apps/frontend/src/app/features/compare/compare.component.ts`
- Modify: `apps/frontend/src/app/features/compare/compare.component.html`

- [ ] **Step 1: Agregar `disclaimerText` constant al TS**

En `apps/frontend/src/app/features/compare/compare.component.ts`, agregar a la clase:

```ts
readonly disclaimerText = 'Las celdas resaltadas en ámbar indican diferencias relevantes entre las versiones seleccionadas.';
```

- [ ] **Step 2: Reemplazar `<app-disclaimer>` final y agregar icono en headers de sección**

En `apps/frontend/src/app/features/compare/compare.component.html`, primero eliminar la línea 275-277 (el `<app-disclaimer>` final).

Luego, en cada `<details>` de secciones (líneas ~183-241), modificar el `<summary>` para incluir el icono al lado del título. Reemplazar el bloque del summary (líneas 189-203):

```html
<summary class="flex items-center justify-between px-5 py-4 cursor-pointer hover:bg-surface-muted transition-colors list-none">
  <div class="flex items-center gap-3">
    <span class="material-symbols-outlined text-brand-700">{{ sectionIcon(s.name) }}</span>
    <h2 class="font-bold text-base">{{ s.label }}</h2>
    <span
      class="material-symbols-outlined text-ink-muted cursor-help text-base"
      [attr.title]="disclaimerText"
      tabindex="0"
      [attr.aria-label]="disclaimerText"
      data-testid="info-icon"
    >info</span>
  </div>
  <span class="material-symbols-outlined group-open:rotate-180 transition-transform">expand_more</span>
</summary>
```

Esto aplica el icono a todas las secciones (specs, precio-anio, equipamiento, mantencion). Si el usuario quiere solo en la primera, ajustar.

- [ ] **Step 3: Quitar import de `DisclaimerComponent`**

En `apps/frontend/src/app/features/compare/compare.component.ts`, eliminar:

```ts
import { DisclaimerComponent } from '../../shared/ui/disclaimer.component';
```

Y del array `imports` del `@Component`:

```ts
imports: [RouterLink],
```

- [ ] **Step 4: Verificar tests**

Run: `npm -w apps/frontend run test -- compare`
Expected: PASS (los tests existentes no dependen del disclaimer).

- [ ] **Step 5: Commit**

```bash
git add apps/frontend/src/app/features/compare
git commit -m "feat(fe): move compare disclaimer to info icon with tooltip"
```

---

### Task 12: `CompareComponent` — carrusel de favoritos

**Files:**
- Modify: `apps/frontend/src/app/features/compare/compare.component.ts`
- Modify: `apps/frontend/src/app/features/compare/compare.component.html`
- Modify: `apps/frontend/src/app/features/compare/compare.component.spec.ts`

- [ ] **Step 1: Agregar estado y métodos al TS**

En `apps/frontend/src/app/features/compare/compare.component.ts`, agregar:

```ts
import { VehicleCardInput } from '../../shared/ui/vehicle-card.component';
```

Y a la clase:

```ts
favoriteModels = signal<VehicleCardInput[]>([]);
carouselOpenFor = signal<string | null>(null); // model.id del popover abierto

async loadFavorites(): Promise<void> {
  if (!this.user()) return;
  try {
    const res = await this.api.get<{ data: VehicleCardInput[] }>('/me/favorites/models');
    this.favoriteModels.set(res.data ?? []);
  } catch {
    /* ignore */
  }
}

isModelInCompare(m: VehicleCardInput): boolean {
  const inStore = new Set(this.compareStore.ids());
  return m.versions.some((v) => inStore.has(v.id));
}

thumbStyle(url: string | null | undefined): string {
  return url ? `url("${url}")` : 'none';
}

toggleCarouselFor(modelId: string): void {
  this.carouselOpenFor.update((c) => (c === modelId ? null : modelId));
}

async addFavoriteVersionToCompare(modelId: string, versionId: string): Promise<void> {
  if (this.compareStore.ids().length >= 3) return;
  this.compareStore.setIds([...this.compareStore.ids(), versionId]);
  this.carouselOpenFor.set(null);
  await this.reloadCompare();
}
```

Modificar `bootstrap()` para cargar favoritos al inicio (junto con la carga normal). Insertar en el inicio del método:

```ts
private async bootstrap(): Promise<void> {
  await this.loadFavorites();
  // ... resto del método existente ...
}
```

- [ ] **Step 2: Agregar el bloque del carrusel al HTML**

En `apps/frontend/src/app/features/compare/compare.component.html`, después del grid de cards (después de línea 179, antes de `<!-- Collapsible sections -->`), agregar:

```html
@if (user() && favoriteModels().length > 0) {
  <section class="mb-8" data-testid="favorites-carousel">
    <header class="flex items-center justify-between mb-3">
      <h2 class="font-bold text-base">Tus favoritos · Agregá uno a la comparación</h2>
    </header>
    <ul class="flex gap-3 overflow-x-auto snap-x snap-mandatory pb-2" role="list">
      @for (m of favoriteModels(); track m.id) {
        @if (!isModelInCompare(m)) {
          <li class="snap-start shrink-0 w-56 bg-surface border border-border rounded-xl p-3 relative"
            [attr.data-testid]="'favorite-carousel-item-' + m.id">
            <div class="aspect-video rounded-lg bg-cover bg-center mb-2 bg-surface-muted"
              [style.background-image]="thumbStyle(m.imageUrl ?? null)"></div>
            <p class="text-xs text-ink-muted">{{ m.brand.name }}</p>
            <h3 class="font-bold text-sm">{{ m.name }}</h3>
            <p class="text-xs text-brand-700 mt-1">
              @if (m.minPrice != null) {
                $ {{ formatPricePlain(m.minPrice) }}
              } @else {
                Sin precio publicado
              }
            </p>
            <button type="button"
              (click)="toggleCarouselFor(m.id); $event.stopPropagation()"
              [attr.aria-expanded]="carouselOpenFor() === m.id"
              [attr.data-testid]="'favorite-carousel-btn-' + m.id"
              class="mt-2 w-full text-xs font-bold bg-brand-600 text-white rounded-full py-1.5 hover:bg-brand-700">
              Agregar versión
            </button>
            @if (carouselOpenFor() === m.id) {
              <ul class="absolute z-30 right-0 top-full mt-1 bg-surface border border-border rounded-lg shadow-e2 p-1 w-56 max-h-60 overflow-auto"
                role="listbox"
                [attr.data-testid]="'favorite-carousel-popover-' + m.id">
                @for (v of m.versions; track v.id) {
                  <li>
                    <button type="button" role="option"
                      (click)="addFavoriteVersionToCompare(m.id, v.id)"
                      [attr.data-testid]="'favorite-carousel-opt-' + v.id"
                      class="block w-full text-left text-xs px-2 py-1.5 rounded hover:bg-surface-muted">
                      <span class="font-semibold">{{ v.name }} {{ v.year }}</span>
                      @if (v.priceClp != null) {
                        <span class="text-ink-muted"> · ${{ formatPricePlain(v.priceClp) }}</span>
                      }
                    </button>
                  </li>
                }
              </ul>
            }
          </li>
        }
      }
    </ul>
  </section>
}
```

- [ ] **Step 3: Click outside para cerrar popover**

Extender el `@HostListener('document:click')` existente en `compare.component.ts`. El método actual solo cierra el swap. Modificarlo:

```ts
@HostListener('document:click', ['$event'])
onDocumentClick(event: MouseEvent): void {
  const target = event.target as HTMLElement | null;
  if (!target) return;
  // close swap
  if (this.swappingFor() !== null && !target.closest('[data-testid^="swap-popover-"]') && !target.closest('[data-testid^="swap-button-"]')) {
    this.closeSwap();
  }
  // close carousel popover
  if (this.carouselOpenFor() !== null && !target.closest('[data-testid^="favorite-carousel-popover-"]') && !target.closest('[data-testid^="favorite-carousel-btn-"]')) {
    this.carouselOpenFor.set(null);
  }
}
```

- [ ] **Step 4: Tests**

Agregar a `compare.component.spec.ts`:

```ts
it('carrusel de favoritos aparece cuando hay user y favoritos', async () => {
  TestBed.resetTestingModule();
  localStorage.clear();
  TestBed.configureTestingModule({
    providers: [
      provideHttpClient(),
      provideHttpClientTesting(),
      provideRouter([]),
      CompareStore,
      AuthService, // ya hay en el módulo usualmente, ajustar según setup actual
      {
        provide: ActivatedRoute,
        useValue: {
          snapshot: {
            queryParamMap: {
              get: () => null,
              has: () => false,
              keys: [],
            },
          },
        },
      },
    ],
  });
  http = TestBed.inject(HttpTestingController);
  store = TestBed.inject(CompareStore);

  // login first
  const auth = TestBed.inject(AuthService);
  (auth as any).currentUser.set({ id: 'u1', email: 'u@test.cl', name: 'U' });

  const fixture = TestBed.createComponent(CompareComponent);
  await fixture.componentInstance.ready;
  // GET /me/favorites/models
  const favReq = http.expectOne((r) => r.url.includes('/me/favorites/models'));
  favReq.flush({ data: [{ id: 'm1', name: 'Yaris', brand: { name: 'Toyota' }, segment: 'HATCHBACK', minPrice: 14000000, versions: [{ id: 'v1', name: 'XLS', year: 2026, priceClp: 14990000 }] }] });
  fixture.detectChanges();

  const carousel = fixture.nativeElement.querySelector('[data-testid="favorites-carousel"]');
  expect(carousel).not.toBeNull();
  expect(carousel.querySelector('[data-testid="favorite-carousel-item-m1"]')).not.toBeNull();
});

it('carrusel oculta modelos cuyas versiones ya están en compare', async () => {
  // similar setup pero con store.hydrateFromUrl('v1') antes
  // y verificar que el item de m1 (que tiene v1) no aparece
});
```

(Ajustar el setup del spec para incluir `AuthService` real o stub. Verificar el spec actual para no romper.)

- [ ] **Step 5: Run tests**

Run: `npm -w apps/frontend run test -- compare`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add apps/frontend/src/app/features/compare
git commit -m "feat(fe): favorites carousel on compare screen"
```

---

## Fase 5 — Frontend: Pantalla Favoritos + Nav

### Task 13: Ruta `/favoritos`

**Files:**
- Modify: `apps/frontend/src/app/app.routes.ts`

- [ ] **Step 1: Agregar ruta**

En `apps/frontend/src/app/app.routes.ts`, después del bloque de `/account/comparisons` (línea 47-53), agregar:

```ts
{
  path: 'favoritos',
  canActivate: [authGuard],
  loadComponent: () =>
    import('./features/favorites/favorites.component').then(
      (m) => m.FavoritesComponent,
    ),
},
```

- [ ] **Step 2: Commit**

```bash
git add apps/frontend/src/app/app.routes.ts
git commit -m "feat(fe): add /favoritos route"
```

---

### Task 14: `FavoritesComponent`

**Files:**
- Create: `apps/frontend/src/app/features/favorites/favorites.component.ts`
- Create: `apps/frontend/src/app/features/favorites/favorites.component.html`
- Create: `apps/frontend/src/app/features/favorites/favorites.component.css`
- Create: `apps/frontend/src/app/features/favorites/favorites.component.spec.ts`

- [ ] **Step 1: Crear `favorites.component.ts`**

```ts
import {
  ChangeDetectionStrategy,
  Component,
  computed,
  inject,
  signal,
} from '@angular/core';
import { Router } from '@angular/router';
import { ApiService } from '../../core/api.service';
import { CompareStore } from '../../core/compare-store.service';
import { FavoritesStore } from '../../core/favorites-store.service';
import {
  VehicleCardComponent,
  VehicleCardInput,
} from '../../shared/ui/vehicle-card.component';

@Component({
  selector: 'app-favorites',
  templateUrl: './favorites.component.html',
  styleUrl: './favorites.component.css',
  imports: [VehicleCardComponent],
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class FavoritesComponent {
  private api = inject(ApiService);
  private compare = inject(CompareStore);
  private favorites = inject(FavoritesStore);
  private router = inject(Router);

  readonly models = signal<VehicleCardInput[]>([]);
  readonly loading = signal(true);
  readonly error = signal<string | null>(null);
  readonly compareMessage = signal<string | null>(null);

  readonly hasItems = computed(() => this.models().length > 0);

  readonly initialLoad: Promise<void>;

  constructor() {
    this.initialLoad = this.load();
  }

  private async load(): Promise<void> {
    this.loading.set(true);
    this.error.set(null);
    try {
      const res = await this.api.get<{ data: VehicleCardInput[] }>('/me/favorites/models');
      this.models.set(res.data ?? []);
    } catch {
      this.error.set('No pudimos cargar tus favoritos.');
    } finally {
      this.loading.set(false);
    }
  }

  isAdded(m: VehicleCardInput): boolean {
    const id = m.defaultVersion?.id;
    return id ? this.compare.ids().includes(id) : false;
  }

  selectedVersionId(m: VehicleCardInput): string | null {
    return m.defaultVersion?.id ?? m.versions[0]?.id ?? null;
  }

  async compareOne(m: VehicleCardInput): Promise<void> {
    if (this.compare.ids().length >= 3) {
      this.compareMessage.set('Máximo 3, limpiá la comparación actual primero.');
      return;
    }
    const vid = this.selectedVersionId(m);
    if (!vid) return;
    this.compare.setIds([...this.compare.ids(), vid]);
    this.compareMessage.set(null);
    await this.router.navigate(['/compare']);
  }

  async compareAllMine(): Promise<void> {
    const ids = this.models()
      .slice(0, 3)
      .map((m) => this.selectedVersionId(m))
      .filter((id): id is string => id != null);
    if (ids.length === 0) return;
    this.compare.setIds(ids);
    await this.router.navigate(['/compare']);
  }

  trackById(_: number, m: VehicleCardInput): string {
    return m.id;
  }
}
```

- [ ] **Step 2: Crear `favorites.component.html`**

```html
<section class="mx-auto max-w-7xl px-4 py-6 md:px-8">
  <header class="mb-8 flex items-end justify-between gap-4 flex-wrap">
    <div>
      <h1 class="text-3xl md:text-4xl font-bold text-ink tracking-tight">Mis favoritos</h1>
      <p class="mt-2 text-sm md:text-base text-ink-muted max-w-2xl">
        Modelos que marcaste con el corazón. Comparálos entre sí o agregalos a la comparación actual.
      </p>
    </div>
    @if (hasItems()) {
      <button type="button"
        (click)="compareAllMine()"
        class="rounded-full bg-brand-600 text-white text-sm font-bold px-5 py-2 hover:bg-brand-700 transition-colors flex items-center gap-2"
        data-testid="compare-all"
        [disabled]="models().length < 2">
        <span class="material-symbols-outlined text-base">compare_arrows</span>
        Comparar mis favoritos ({{ Math.min(models().length, 3) }})
      </button>
    }
  </header>

  @if (loading()) {
    <p class="text-sm text-brand-600" data-testid="loading">Cargando…</p>
  }

  @if (error(); as msg) {
    <div class="rounded-md border border-warn bg-warn-light px-3 py-2 text-sm text-warn-dark" role="alert" data-testid="error">
      {{ msg }}
    </div>
  }

  @if (compareMessage(); as msg) {
    <div class="mb-4 rounded-md border border-warn bg-warn-light px-3 py-2 text-sm text-warn-dark" role="alert" data-testid="compare-message">
      {{ msg }}
    </div>
  }

  @if (!loading() && !hasItems() && !error()) {
    <div class="rounded-xl border border-border bg-surface p-12 text-center shadow-sm" data-testid="empty">
      <p class="font-bold text-lg text-ink">Aún no tenés favoritos</p>
      <p class="mt-2 text-sm text-ink-muted">
        Marcá con el corazón desde el catálogo para verlos acá.
      </p>
      <a routerLink="/catalogo" class="mt-4 inline-block rounded-full bg-brand-600 px-5 py-2 text-sm font-bold text-white hover:bg-brand-700 transition-colors">
        Ir al catálogo
      </a>
    </div>
  }

  @if (hasItems()) {
    <ul class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4" data-testid="favorites-grid">
      @for (m of models(); track trackById($index, m)) {
        <li class="flex flex-col gap-2">
          <app-vehicle-card
            [model]="m"
            [added]="isAdded(m)"
            [selectedVersionId]="selectedVersionId(m)"
          />
          <button type="button"
            (click)="compareOne(m)"
            class="w-full text-sm font-bold bg-brand-600 text-white rounded-full py-2 hover:bg-brand-700 transition-colors flex items-center justify-center gap-2"
            [attr.data-testid]="'compare-one-' + m.id">
            <span class="material-symbols-outlined text-base">compare_arrows</span>
            Comparar
          </button>
        </li>
      }
    </ul>
  }
</section>
```

(Nota: `Math.min` no está disponible en el template. Cambiar a un método o computed.)

Reemplazar `Math.min(models().length, 3)` por `compareCount()` que es un computed:

En TS, agregar:

```ts
readonly compareCount = computed(() => Math.min(this.models().length, 3));
```

Y en HTML:

```html
Comparar mis favoritos ({{ compareCount() }})
```

- [ ] **Step 3: Crear `favorites.component.css`**

```css
:host { display: block; }
```

- [ ] **Step 4: Crear `favorites.component.spec.ts`**

```ts
import { TestBed } from '@angular/core/testing';
import { provideHttpClient } from '@angular/common/http';
import { provideHttpClientTesting, HttpTestingController } from '@angular/common/http/testing';
import { provideRouter } from '@angular/router';
import { signal } from '@angular/core';
import { FavoritesComponent } from './favorites.component';
import { AuthService, type User } from '../../core/auth.service';
import { CompareStore } from '../../core/compare-store.service';
import { FavoritesStore } from '../../core/favorites-store.service';

class AuthServiceStub {
  currentUser = signal<User | null>({ id: 'u1', email: 'u@test.cl', name: 'U' });
}
class FavoritesStoreStub {
  isFavorite(): boolean { return true; }
  async toggle(): Promise<void> {}
  load(): Promise<void> { return Promise.resolve(); }
}

describe('FavoritesComponent', () => {
  let http: HttpTestingController;
  let store: CompareStore;

  beforeEach(() => {
    TestBed.resetTestingModule();
    localStorage.clear();
    TestBed.configureTestingModule({
      providers: [
        provideHttpClient(),
        provideHttpClientTesting(),
        provideRouter([]),
        { provide: AuthService, useClass: AuthServiceStub },
        { provide: FavoritesStore, useClass: FavoritesStoreStub },
        CompareStore,
        FavoritesComponent,
      ],
    });
    http = TestBed.inject(HttpTestingController);
    store = TestBed.inject(CompareStore);
  });

  afterEach(() => {
    http.verify();
    localStorage.clear();
  });

  it('carga inicial llama GET /me/favorites/models', async () => {
    const fixture = TestBed.createComponent(FavoritesComponent);
    fixture.detectChanges();
    const req = http.expectOne((r) => r.url.includes('/me/favorites/models'));
    req.flush({ data: [{ id: 'm1', name: 'Yaris', brand: { name: 'Toyota' }, segment: 'HATCHBACK', minPrice: 14000000, defaultVersion: { id: 'v1', name: 'XLS', priceClp: 14990000, year: 2026 }, versions: [{ id: 'v1', name: 'XLS', priceClp: 14990000, year: 2026 }] }] });
    await fixture.componentInstance.initialLoad;
    fixture.detectChanges();
    const grid = fixture.nativeElement.querySelector('[data-testid="favorites-grid"]');
    expect(grid).not.toBeNull();
  });

  it('muestra empty state cuando no hay favoritos', async () => {
    const fixture = TestBed.createComponent(FavoritesComponent);
    fixture.detectChanges();
    http.expectOne((r) => r.url.includes('/me/favorites/models')).flush({ data: [] });
    await fixture.componentInstance.initialLoad;
    fixture.detectChanges();
    const empty = fixture.nativeElement.querySelector('[data-testid="empty"]');
    expect(empty).not.toBeNull();
  });

  it('click Comparar agrega defaultVersion al store y navega', async () => {
    const fixture = TestBed.createComponent(FavoritesComponent);
    fixture.detectChanges();
    http.expectOne((r) => r.url.includes('/me/favorites/models')).flush({
      data: [{ id: 'm1', name: 'Yaris', brand: { name: 'Toyota' }, segment: 'HATCHBACK', minPrice: 14000000, defaultVersion: { id: 'v1', name: 'XLS', priceClp: 14990000, year: 2026 }, versions: [{ id: 'v1', name: 'XLS', priceClp: 14990000, year: 2026 }] }],
    });
    await fixture.componentInstance.initialLoad;
    fixture.detectChanges();

    const btn: HTMLButtonElement = fixture.nativeElement.querySelector('[data-testid="compare-one-m1"]');
    btn.click();
    fixture.detectChanges();
    // navegación es async — esperar
    await Promise.resolve();
    await Promise.resolve();
    expect(store.ids()).toEqual(['v1']);
  });
});
```

- [ ] **Step 5: Run tests**

Run: `npm -w apps/frontend run test -- favorites`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add apps/frontend/src/app/features/favorites
git commit -m "feat(fe): favorites screen with grid and compare actions"
```

---

### Task 15: Nav link Favoritos

**Files:**
- Modify: `apps/frontend/src/app/layout/top-nav-bar.component.ts`
- Modify: `apps/frontend/src/app/layout/top-nav-bar.component.spec.ts` (extender)

- [ ] **Step 1: Agregar link al array navLinks**

En `apps/frontend/src/app/layout/top-nav-bar.component.ts`, modificar el método `navLinks`:

```ts
readonly navLinks = computed<NavLink[]>(() => {
  const u = this.user();
  const base: NavLink[] = [
    { path: '/', label: 'Inicio', icon: 'home', exact: true },
    { path: '/catalogo', label: 'Catálogo', icon: 'directions_car' },
    { path: '/favoritos', label: 'Favoritos', icon: 'favorite' },
    { path: '/compare', label: 'Comparar', icon: 'compare_arrows' },
  ];
  if (u) base.push({ path: '/account/comparisons', label: 'Mis comparaciones', icon: 'bookmarks' });
  return base;
});
```

- [ ] **Step 2: Agregar test**

En `top-nav-bar.component.spec.ts`, agregar:

```ts
it('muestra link Favoritos cuando hay user', () => {
  (authStub as any).currentUser.set({ id: 'u1', email: 'u@test.cl', name: 'U' });
  fixture.detectChanges();
  const favLink = fixture.nativeElement.querySelector('a[href="/favoritos"]');
  expect(favLink).not.toBeNull();
});

it('oculta link Favoritos cuando no hay user', () => {
  (authStub as any).currentUser.set(null);
  fixture.detectChanges();
  const favLink = fixture.nativeElement.querySelector('a[href="/favoritos"]');
  expect(favLink).toBeNull();
});
```

(Ajustar `authStub` según el spec actual — verificar qué ya existe.)

- [ ] **Step 3: Run tests**

Run: `npm -w apps/frontend run test -- top-nav-bar`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add apps/frontend/src/app/layout
git commit -m "feat(fe): add Favoritos link to top nav"
```

---

## Fase 6 — Verificación final

### Task 16: Smoke test E2E + commit final

- [ ] **Step 1: Aplicar migración y seed limpio**

```bash
npm run db:reset
npm run db:seed
```

- [ ] **Step 2: Levantar backend y frontend**

```bash
npm run dev
```

Esperado: backend en :3000, frontend en :4200.

- [ ] **Step 3: Flujo manual completo**

1. Abrir `/catalogo`. Verificar que aparecen los filtros nuevos (Marca, Transmisión, Combustible, Año, Potencia, Consumo) y los controles de orden. Aplicar `Transmisión = Automática` y `Ordenar por = Precio asc` — la grilla refleja el orden.
2. Sin estar logueado, los botones corazón en cada card están deshabilitados con tooltip "Inicia sesión para guardar favoritos".
3. Registrarse (botón "Crear cuenta" en la nav).
4. Marcar 3 modelos como favoritos (corazón lleno en cada card).
5. Ir a `/favoritos` (link aparece en la nav). El grid muestra los 3 favoritos. El botón "Comparar mis favoritos (3)" está habilitado. Click → navega a `/compare` con 3 versiones.
6. En `/compare`, click "Guardar comparación" → ve banner "Ver enlace público".
7. Sin cambiar las versiones, click "Guardar comparación" otra vez → banner cambia a "Ya tenés esta comparación guardada. [Ver enlace público]".
8. Hover en el icono `info` al lado del título de la sección "Especificaciones" → tooltip con el texto del disclaimer aparece.
9. Volver a `/catalogo` y desmarcar 2 favoritos. Volver a `/favoritos` → grid muestra solo 1 favorito; botón "Comparar mis favoritos" disabled.

- [ ] **Step 4: Run all tests**

```bash
npm test
```

Expected: todos los tests pasan.

```bash
npm run test:e2e
```

Expected: todos los e2e pasan.

- [ ] **Step 5: Commit final**

```bash
git add -A
git commit -m "chore: smoke test v2 features"
```

---

## Self-review

**1. Spec coverage:**
- §1 modelo datos → Task 0 ✓
- §2.1-2.2 endpoints favoritos → Tasks 3,4 ✓
- §2.3 dedup → Task 2 ✓
- §2.4 sort/order → Task 5 ✓
- §3 catálogo → Task 8 ✓
- §4 FavoritesStore → Task 6 ✓
- §5 VehicleCard extendido → Task 7 ✓
- §6.1 dedup frontend → Task 10 ✓
- §6.2 info icon → Task 11 ✓
- §6.3 carrusel → Task 12 ✓
- §7 pantalla Favoritos → Tasks 13,14 ✓
- §7.5 nav → Task 15 ✓

**2. Placeholder scan:**
- No TBD/TODO. Cada paso tiene código concreto o comando exacto.
- `Math.min` referencia en Task 14 fue corregido con un `computed` explícito.
- `formatPricePlain` ya existe en `compare.component.ts` (línea 376); reutilizado en Task 12.

**3. Type consistency:**
- `VehicleCardInput` definido y usado en Tasks 7, 8, 12, 14.
- `FavoriteModelCard` (backend) coincide con shape que el frontend espera.
- `versionsHash` siempre `string`, calculado con `crypto.createHash('sha1').update(sorted.join(',')).digest('hex')`.
- `savedSlug` y `duplicateSlug` son signals string|null consistentes.

**4. Orden de ejecución:**
- Task 0 (schema) bloquea 1-5.
- Tasks 1-5 son backend, todas independientes entre sí (pueden paralelizarse).
- Task 6 (FavoritesStore) bloquea Task 7 (que lo usa) y Tasks 8-15 que dependen de auth.
- Task 7 (VehicleCard) bloquea Tasks 9, 12, 14.
- Task 8 (Catalog) independiente.
- Tasks 10-12 son todas sobre `compare.component.ts` y modifican el mismo archivo → ejecutar en orden.
- Task 13 (ruta) bloquea Task 14 (componente que la usa).
- Task 15 (nav) independiente.

---

## Resumen

- **Total tasks**: 16
- **Commits**: 16
- **Fases**: 6 (schema, backend, frontend-store, frontend-catálogo/landing, frontend-comparar, frontend-favoritos, verificación)
- **Riesgos cubiertos**: backfill versionsHash, race en dedup, pgcrypto no disponible, effect timing, sort en memoria rompe paginación (aceptado MVP).